// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:recipe_serializer`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:recipe_serializer` (21 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`RecipeSerializer::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`RecipeSerializer::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum RecipeSerializer {
    /// `minecraft:crafting_shaped` (id 0).
    CraftingShaped = 0,
    /// `minecraft:crafting_shapeless` (id 1).
    CraftingShapeless = 1,
    /// `minecraft:crafting_dye` (id 2).
    CraftingDye = 2,
    /// `minecraft:crafting_imbue` (id 3).
    CraftingImbue = 3,
    /// `minecraft:crafting_transmute` (id 4).
    CraftingTransmute = 4,
    /// `minecraft:crafting_decorated_pot` (id 5).
    CraftingDecoratedPot = 5,
    /// `minecraft:crafting_special_bookcloning` (id 6).
    CraftingSpecialBookcloning = 6,
    /// `minecraft:crafting_special_mapextending` (id 7).
    CraftingSpecialMapextending = 7,
    /// `minecraft:crafting_special_firework_rocket` (id 8).
    CraftingSpecialFireworkRocket = 8,
    /// `minecraft:crafting_special_firework_star` (id 9).
    CraftingSpecialFireworkStar = 9,
    /// `minecraft:crafting_special_firework_star_fade` (id 10).
    CraftingSpecialFireworkStarFade = 10,
    /// `minecraft:crafting_special_bannerduplicate` (id 11).
    CraftingSpecialBannerduplicate = 11,
    /// `minecraft:crafting_special_shielddecoration` (id 12).
    CraftingSpecialShielddecoration = 12,
    /// `minecraft:crafting_special_repairitem` (id 13).
    CraftingSpecialRepairitem = 13,
    /// `minecraft:smelting` (id 14).
    Smelting = 14,
    /// `minecraft:blasting` (id 15).
    Blasting = 15,
    /// `minecraft:smoking` (id 16).
    Smoking = 16,
    /// `minecraft:campfire_cooking` (id 17).
    CampfireCooking = 17,
    /// `minecraft:stonecutting` (id 18).
    Stonecutting = 18,
    /// `minecraft:smithing_transform` (id 19).
    SmithingTransform = 19,
    /// `minecraft:smithing_trim` (id 20).
    SmithingTrim = 20,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`RecipeSerializer::name`] is one load from a static.
/// `crate::generated::registry::RECIPE_SERIALIZER` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 21] = [
    "minecraft:crafting_shaped",
    "minecraft:crafting_shapeless",
    "minecraft:crafting_dye",
    "minecraft:crafting_imbue",
    "minecraft:crafting_transmute",
    "minecraft:crafting_decorated_pot",
    "minecraft:crafting_special_bookcloning",
    "minecraft:crafting_special_mapextending",
    "minecraft:crafting_special_firework_rocket",
    "minecraft:crafting_special_firework_star",
    "minecraft:crafting_special_firework_star_fade",
    "minecraft:crafting_special_bannerduplicate",
    "minecraft:crafting_special_shielddecoration",
    "minecraft:crafting_special_repairitem",
    "minecraft:smelting",
    "minecraft:blasting",
    "minecraft:smoking",
    "minecraft:campfire_cooking",
    "minecraft:stonecutting",
    "minecraft:smithing_transform",
    "minecraft:smithing_trim",
];

/// Discriminants ordered by name, so [`RecipeSerializer::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 21 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 21] = [
    15, 17, 5, 2, 3, 0, 1, 11, 6, 8, 9, 10, 7, 13, 12, 4, 14, 19, 20, 16, 18,
];

impl RecipeSerializer {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::CraftingShaped,
        Self::CraftingShapeless,
        Self::CraftingDye,
        Self::CraftingImbue,
        Self::CraftingTransmute,
        Self::CraftingDecoratedPot,
        Self::CraftingSpecialBookcloning,
        Self::CraftingSpecialMapextending,
        Self::CraftingSpecialFireworkRocket,
        Self::CraftingSpecialFireworkStar,
        Self::CraftingSpecialFireworkStarFade,
        Self::CraftingSpecialBannerduplicate,
        Self::CraftingSpecialShielddecoration,
        Self::CraftingSpecialRepairitem,
        Self::Smelting,
        Self::Blasting,
        Self::Smoking,
        Self::CampfireCooking,
        Self::Stonecutting,
        Self::SmithingTransform,
        Self::SmithingTrim,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 21;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:recipe_serializer";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `minecraft:crafting_shaped`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `RecipeSerializer` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for RecipeSerializer {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(RecipeSerializer::ALL[20].id().0 == 20);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<RecipeSerializer>() == 1);
