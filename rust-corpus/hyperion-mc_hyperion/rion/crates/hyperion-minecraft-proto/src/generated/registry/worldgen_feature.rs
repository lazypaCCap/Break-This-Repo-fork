// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:worldgen/feature`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:worldgen/feature` (63 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`WorldgenFeature::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`WorldgenFeature::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum WorldgenFeature {
    /// `minecraft:no_op` (id 0).
    NoOp = 0,
    /// `minecraft:tree` (id 1).
    Tree = 1,
    /// `minecraft:fallen_tree` (id 2).
    FallenTree = 2,
    /// `minecraft:block_pile` (id 3).
    BlockPile = 3,
    /// `minecraft:spring_feature` (id 4).
    SpringFeature = 4,
    /// `minecraft:chorus_plant` (id 5).
    ChorusPlant = 5,
    /// `minecraft:replace_single_block` (id 6).
    ReplaceSingleBlock = 6,
    /// `minecraft:void_start_platform` (id 7).
    VoidStartPlatform = 7,
    /// `minecraft:desert_well` (id 8).
    DesertWell = 8,
    /// `minecraft:fossil` (id 9).
    Fossil = 9,
    /// `minecraft:huge_red_mushroom` (id 10).
    HugeRedMushroom = 10,
    /// `minecraft:huge_brown_mushroom` (id 11).
    HugeBrownMushroom = 11,
    /// `minecraft:spike` (id 12).
    Spike = 12,
    /// `minecraft:glowstone_blob` (id 13).
    GlowstoneBlob = 13,
    /// `minecraft:freeze_top_layer` (id 14).
    FreezeTopLayer = 14,
    /// `minecraft:vines` (id 15).
    Vines = 15,
    /// `minecraft:block_column` (id 16).
    BlockColumn = 16,
    /// `minecraft:vegetation_patch` (id 17).
    VegetationPatch = 17,
    /// `minecraft:waterlogged_vegetation_patch` (id 18).
    WaterloggedVegetationPatch = 18,
    /// `minecraft:root_system` (id 19).
    RootSystem = 19,
    /// `minecraft:multiface_growth` (id 20).
    MultifaceGrowth = 20,
    /// `minecraft:underwater_magma` (id 21).
    UnderwaterMagma = 21,
    /// `minecraft:monster_room` (id 22).
    MonsterRoom = 22,
    /// `minecraft:blue_ice` (id 23).
    BlueIce = 23,
    /// `minecraft:iceberg` (id 24).
    Iceberg = 24,
    /// `minecraft:block_blob` (id 25).
    BlockBlob = 25,
    /// `minecraft:disk` (id 26).
    Disk = 26,
    /// `minecraft:lake` (id 27).
    Lake = 27,
    /// `minecraft:ore` (id 28).
    Ore = 28,
    /// `minecraft:end_platform` (id 29).
    EndPlatform = 29,
    /// `minecraft:end_spike` (id 30).
    EndSpike = 30,
    /// `minecraft:end_island` (id 31).
    EndIsland = 31,
    /// `minecraft:end_gateway` (id 32).
    EndGateway = 32,
    /// `minecraft:seagrass` (id 33).
    Seagrass = 33,
    /// `minecraft:kelp` (id 34).
    Kelp = 34,
    /// `minecraft:coral_tree` (id 35).
    CoralTree = 35,
    /// `minecraft:coral_mushroom` (id 36).
    CoralMushroom = 36,
    /// `minecraft:coral_claw` (id 37).
    CoralClaw = 37,
    /// `minecraft:sea_pickle` (id 38).
    SeaPickle = 38,
    /// `minecraft:simple_block` (id 39).
    SimpleBlock = 39,
    /// `minecraft:bamboo` (id 40).
    Bamboo = 40,
    /// `minecraft:huge_fungus` (id 41).
    HugeFungus = 41,
    /// `minecraft:nether_forest_vegetation` (id 42).
    NetherForestVegetation = 42,
    /// `minecraft:weeping_vines` (id 43).
    WeepingVines = 43,
    /// `minecraft:twisting_vines` (id 44).
    TwistingVines = 44,
    /// `minecraft:basalt_columns` (id 45).
    BasaltColumns = 45,
    /// `minecraft:delta_feature` (id 46).
    DeltaFeature = 46,
    /// `minecraft:netherrack_replace_blobs` (id 47).
    NetherrackReplaceBlobs = 47,
    /// `minecraft:fill_layer` (id 48).
    FillLayer = 48,
    /// `minecraft:bonus_chest` (id 49).
    BonusChest = 49,
    /// `minecraft:basalt_pillar` (id 50).
    BasaltPillar = 50,
    /// `minecraft:scattered_ore` (id 51).
    ScatteredOre = 51,
    /// `minecraft:random_selector` (id 52).
    RandomSelector = 52,
    /// `minecraft:weighted_random_selector` (id 53).
    WeightedRandomSelector = 53,
    /// `minecraft:simple_random_selector` (id 54).
    SimpleRandomSelector = 54,
    /// `minecraft:random_boolean_selector` (id 55).
    RandomBooleanSelector = 55,
    /// `minecraft:sequence` (id 56).
    Sequence = 56,
    /// `minecraft:template` (id 57).
    Template = 57,
    /// `minecraft:geode` (id 58).
    Geode = 58,
    /// `minecraft:speleothem_cluster` (id 59).
    SpeleothemCluster = 59,
    /// `minecraft:large_dripstone` (id 60).
    LargeDripstone = 60,
    /// `minecraft:speleothem` (id 61).
    Speleothem = 61,
    /// `minecraft:sculk_patch` (id 62).
    SculkPatch = 62,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`WorldgenFeature::name`] is one load from a static.
/// `crate::generated::registry::WORLDGEN_FEATURE` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 63] = [
    "minecraft:no_op",
    "minecraft:tree",
    "minecraft:fallen_tree",
    "minecraft:block_pile",
    "minecraft:spring_feature",
    "minecraft:chorus_plant",
    "minecraft:replace_single_block",
    "minecraft:void_start_platform",
    "minecraft:desert_well",
    "minecraft:fossil",
    "minecraft:huge_red_mushroom",
    "minecraft:huge_brown_mushroom",
    "minecraft:spike",
    "minecraft:glowstone_blob",
    "minecraft:freeze_top_layer",
    "minecraft:vines",
    "minecraft:block_column",
    "minecraft:vegetation_patch",
    "minecraft:waterlogged_vegetation_patch",
    "minecraft:root_system",
    "minecraft:multiface_growth",
    "minecraft:underwater_magma",
    "minecraft:monster_room",
    "minecraft:blue_ice",
    "minecraft:iceberg",
    "minecraft:block_blob",
    "minecraft:disk",
    "minecraft:lake",
    "minecraft:ore",
    "minecraft:end_platform",
    "minecraft:end_spike",
    "minecraft:end_island",
    "minecraft:end_gateway",
    "minecraft:seagrass",
    "minecraft:kelp",
    "minecraft:coral_tree",
    "minecraft:coral_mushroom",
    "minecraft:coral_claw",
    "minecraft:sea_pickle",
    "minecraft:simple_block",
    "minecraft:bamboo",
    "minecraft:huge_fungus",
    "minecraft:nether_forest_vegetation",
    "minecraft:weeping_vines",
    "minecraft:twisting_vines",
    "minecraft:basalt_columns",
    "minecraft:delta_feature",
    "minecraft:netherrack_replace_blobs",
    "minecraft:fill_layer",
    "minecraft:bonus_chest",
    "minecraft:basalt_pillar",
    "minecraft:scattered_ore",
    "minecraft:random_selector",
    "minecraft:weighted_random_selector",
    "minecraft:simple_random_selector",
    "minecraft:random_boolean_selector",
    "minecraft:sequence",
    "minecraft:template",
    "minecraft:geode",
    "minecraft:speleothem_cluster",
    "minecraft:large_dripstone",
    "minecraft:speleothem",
    "minecraft:sculk_patch",
];

/// Discriminants ordered by name, so [`WorldgenFeature::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 63 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 63] = [
    40, 45, 50, 25, 16, 3, 23, 49, 5, 37, 36, 35, 46, 8, 26, 32, 31, 29, 30, 2, 48, 9, 14, 58, 13,
    11, 41, 10, 24, 34, 27, 60, 22, 20, 42, 47, 0, 28, 55, 52, 6, 19, 51, 62, 38, 33, 56, 39, 54,
    61, 59, 12, 4, 57, 1, 44, 21, 17, 15, 7, 18, 43, 53,
];

impl WorldgenFeature {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::NoOp,
        Self::Tree,
        Self::FallenTree,
        Self::BlockPile,
        Self::SpringFeature,
        Self::ChorusPlant,
        Self::ReplaceSingleBlock,
        Self::VoidStartPlatform,
        Self::DesertWell,
        Self::Fossil,
        Self::HugeRedMushroom,
        Self::HugeBrownMushroom,
        Self::Spike,
        Self::GlowstoneBlob,
        Self::FreezeTopLayer,
        Self::Vines,
        Self::BlockColumn,
        Self::VegetationPatch,
        Self::WaterloggedVegetationPatch,
        Self::RootSystem,
        Self::MultifaceGrowth,
        Self::UnderwaterMagma,
        Self::MonsterRoom,
        Self::BlueIce,
        Self::Iceberg,
        Self::BlockBlob,
        Self::Disk,
        Self::Lake,
        Self::Ore,
        Self::EndPlatform,
        Self::EndSpike,
        Self::EndIsland,
        Self::EndGateway,
        Self::Seagrass,
        Self::Kelp,
        Self::CoralTree,
        Self::CoralMushroom,
        Self::CoralClaw,
        Self::SeaPickle,
        Self::SimpleBlock,
        Self::Bamboo,
        Self::HugeFungus,
        Self::NetherForestVegetation,
        Self::WeepingVines,
        Self::TwistingVines,
        Self::BasaltColumns,
        Self::DeltaFeature,
        Self::NetherrackReplaceBlobs,
        Self::FillLayer,
        Self::BonusChest,
        Self::BasaltPillar,
        Self::ScatteredOre,
        Self::RandomSelector,
        Self::WeightedRandomSelector,
        Self::SimpleRandomSelector,
        Self::RandomBooleanSelector,
        Self::Sequence,
        Self::Template,
        Self::Geode,
        Self::SpeleothemCluster,
        Self::LargeDripstone,
        Self::Speleothem,
        Self::SculkPatch,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 63;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:worldgen/feature";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `minecraft:no_op`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `WorldgenFeature` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for WorldgenFeature {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(WorldgenFeature::ALL[62].id().0 == 62);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<WorldgenFeature>() == 1);
