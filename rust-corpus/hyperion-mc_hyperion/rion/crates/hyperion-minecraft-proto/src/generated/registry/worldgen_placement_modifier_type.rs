// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:worldgen/placement_modifier_type`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:worldgen/placement_modifier_type` (15 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`WorldgenPlacementModifierType::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`WorldgenPlacementModifierType::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum WorldgenPlacementModifierType {
    /// `minecraft:block_predicate_filter` (id 0).
    BlockPredicateFilter = 0,
    /// `minecraft:rarity_filter` (id 1).
    RarityFilter = 1,
    /// `minecraft:surface_relative_threshold_filter` (id 2).
    SurfaceRelativeThresholdFilter = 2,
    /// `minecraft:surface_water_depth_filter` (id 3).
    SurfaceWaterDepthFilter = 3,
    /// `minecraft:biome` (id 4).
    Biome = 4,
    /// `minecraft:count` (id 5).
    Count = 5,
    /// `minecraft:noise_based_count` (id 6).
    NoiseBasedCount = 6,
    /// `minecraft:noise_threshold_count` (id 7).
    NoiseThresholdCount = 7,
    /// `minecraft:count_on_every_layer` (id 8).
    CountOnEveryLayer = 8,
    /// `minecraft:environment_scan` (id 9).
    EnvironmentScan = 9,
    /// `minecraft:heightmap` (id 10).
    Heightmap = 10,
    /// `minecraft:height_range` (id 11).
    HeightRange = 11,
    /// `minecraft:in_square` (id 12).
    InSquare = 12,
    /// `minecraft:random_offset` (id 13).
    RandomOffset = 13,
    /// `minecraft:fixed_placement` (id 14).
    FixedPlacement = 14,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`WorldgenPlacementModifierType::name`] is one load from a static.
/// `crate::generated::registry::WORLDGEN_PLACEMENT_MODIFIER_TYPE` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 15] = [
    "minecraft:block_predicate_filter",
    "minecraft:rarity_filter",
    "minecraft:surface_relative_threshold_filter",
    "minecraft:surface_water_depth_filter",
    "minecraft:biome",
    "minecraft:count",
    "minecraft:noise_based_count",
    "minecraft:noise_threshold_count",
    "minecraft:count_on_every_layer",
    "minecraft:environment_scan",
    "minecraft:heightmap",
    "minecraft:height_range",
    "minecraft:in_square",
    "minecraft:random_offset",
    "minecraft:fixed_placement",
];

/// Discriminants ordered by name, so [`WorldgenPlacementModifierType::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 15 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 15] = [4, 0, 5, 8, 9, 14, 11, 10, 12, 6, 7, 13, 1, 2, 3];

impl WorldgenPlacementModifierType {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::BlockPredicateFilter,
        Self::RarityFilter,
        Self::SurfaceRelativeThresholdFilter,
        Self::SurfaceWaterDepthFilter,
        Self::Biome,
        Self::Count,
        Self::NoiseBasedCount,
        Self::NoiseThresholdCount,
        Self::CountOnEveryLayer,
        Self::EnvironmentScan,
        Self::Heightmap,
        Self::HeightRange,
        Self::InSquare,
        Self::RandomOffset,
        Self::FixedPlacement,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 15;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:worldgen/placement_modifier_type";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `minecraft:block_predicate_filter`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `WorldgenPlacementModifierType` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for WorldgenPlacementModifierType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(WorldgenPlacementModifierType::ALL[14].id().0 == 14);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<WorldgenPlacementModifierType>() == 1);
