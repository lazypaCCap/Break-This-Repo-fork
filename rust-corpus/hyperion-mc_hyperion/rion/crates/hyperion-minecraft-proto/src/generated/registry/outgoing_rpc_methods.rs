// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:outgoing_rpc_methods`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:outgoing_rpc_methods` (17 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`OutgoingRpcMethods::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`OutgoingRpcMethods::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum OutgoingRpcMethods {
    /// `minecraft:notification/server/started` (id 0).
    NotificationServerStarted = 0,
    /// `minecraft:notification/server/stopping` (id 1).
    NotificationServerStopping = 1,
    /// `minecraft:notification/server/saving` (id 2).
    NotificationServerSaving = 2,
    /// `minecraft:notification/server/saved` (id 3).
    NotificationServerSaved = 3,
    /// `minecraft:notification/server/activity` (id 4).
    NotificationServerActivity = 4,
    /// `minecraft:notification/players/joined` (id 5).
    NotificationPlayersJoined = 5,
    /// `minecraft:notification/players/left` (id 6).
    NotificationPlayersLeft = 6,
    /// `minecraft:notification/operators/added` (id 7).
    NotificationOperatorsAdded = 7,
    /// `minecraft:notification/operators/removed` (id 8).
    NotificationOperatorsRemoved = 8,
    /// `minecraft:notification/allowlist/added` (id 9).
    NotificationAllowlistAdded = 9,
    /// `minecraft:notification/allowlist/removed` (id 10).
    NotificationAllowlistRemoved = 10,
    /// `minecraft:notification/ip_bans/added` (id 11).
    NotificationIpBansAdded = 11,
    /// `minecraft:notification/ip_bans/removed` (id 12).
    NotificationIpBansRemoved = 12,
    /// `minecraft:notification/bans/added` (id 13).
    NotificationBansAdded = 13,
    /// `minecraft:notification/bans/removed` (id 14).
    NotificationBansRemoved = 14,
    /// `minecraft:notification/gamerules/updated` (id 15).
    NotificationGamerulesUpdated = 15,
    /// `minecraft:notification/server/status` (id 16).
    NotificationServerStatus = 16,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`OutgoingRpcMethods::name`] is one load from a static.
/// `crate::generated::registry::OUTGOING_RPC_METHODS` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 17] = [
    "minecraft:notification/server/started",
    "minecraft:notification/server/stopping",
    "minecraft:notification/server/saving",
    "minecraft:notification/server/saved",
    "minecraft:notification/server/activity",
    "minecraft:notification/players/joined",
    "minecraft:notification/players/left",
    "minecraft:notification/operators/added",
    "minecraft:notification/operators/removed",
    "minecraft:notification/allowlist/added",
    "minecraft:notification/allowlist/removed",
    "minecraft:notification/ip_bans/added",
    "minecraft:notification/ip_bans/removed",
    "minecraft:notification/bans/added",
    "minecraft:notification/bans/removed",
    "minecraft:notification/gamerules/updated",
    "minecraft:notification/server/status",
];

/// Discriminants ordered by name, so [`OutgoingRpcMethods::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 17 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 17] = [9, 10, 13, 14, 15, 11, 12, 7, 8, 5, 6, 4, 3, 2, 0, 16, 1];

impl OutgoingRpcMethods {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::NotificationServerStarted,
        Self::NotificationServerStopping,
        Self::NotificationServerSaving,
        Self::NotificationServerSaved,
        Self::NotificationServerActivity,
        Self::NotificationPlayersJoined,
        Self::NotificationPlayersLeft,
        Self::NotificationOperatorsAdded,
        Self::NotificationOperatorsRemoved,
        Self::NotificationAllowlistAdded,
        Self::NotificationAllowlistRemoved,
        Self::NotificationIpBansAdded,
        Self::NotificationIpBansRemoved,
        Self::NotificationBansAdded,
        Self::NotificationBansRemoved,
        Self::NotificationGamerulesUpdated,
        Self::NotificationServerStatus,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 17;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:outgoing_rpc_methods";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `minecraft:notification/server/started`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `OutgoingRpcMethods` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for OutgoingRpcMethods {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(OutgoingRpcMethods::ALL[16].id().0 == 16);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<OutgoingRpcMethods>() == 1);
