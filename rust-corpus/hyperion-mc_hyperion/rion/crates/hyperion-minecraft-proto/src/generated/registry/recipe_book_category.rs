// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:recipe_book_category`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:recipe_book_category` (13 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`RecipeBookCategory::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`RecipeBookCategory::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum RecipeBookCategory {
    /// `minecraft:crafting_building_blocks` (id 0).
    CraftingBuildingBlocks = 0,
    /// `minecraft:crafting_redstone` (id 1).
    CraftingRedstone = 1,
    /// `minecraft:crafting_equipment` (id 2).
    CraftingEquipment = 2,
    /// `minecraft:crafting_misc` (id 3).
    CraftingMisc = 3,
    /// `minecraft:furnace_food` (id 4).
    FurnaceFood = 4,
    /// `minecraft:furnace_blocks` (id 5).
    FurnaceBlocks = 5,
    /// `minecraft:furnace_misc` (id 6).
    FurnaceMisc = 6,
    /// `minecraft:blast_furnace_blocks` (id 7).
    BlastFurnaceBlocks = 7,
    /// `minecraft:blast_furnace_misc` (id 8).
    BlastFurnaceMisc = 8,
    /// `minecraft:smoker_food` (id 9).
    SmokerFood = 9,
    /// `minecraft:stonecutter` (id 10).
    Stonecutter = 10,
    /// `minecraft:smithing` (id 11).
    Smithing = 11,
    /// `minecraft:campfire` (id 12).
    Campfire = 12,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`RecipeBookCategory::name`] is one load from a static.
/// `crate::generated::registry::RECIPE_BOOK_CATEGORY` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 13] = [
    "minecraft:crafting_building_blocks",
    "minecraft:crafting_redstone",
    "minecraft:crafting_equipment",
    "minecraft:crafting_misc",
    "minecraft:furnace_food",
    "minecraft:furnace_blocks",
    "minecraft:furnace_misc",
    "minecraft:blast_furnace_blocks",
    "minecraft:blast_furnace_misc",
    "minecraft:smoker_food",
    "minecraft:stonecutter",
    "minecraft:smithing",
    "minecraft:campfire",
];

/// Discriminants ordered by name, so [`RecipeBookCategory::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 13 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 13] = [7, 8, 12, 0, 2, 3, 1, 5, 4, 6, 11, 9, 10];

impl RecipeBookCategory {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::CraftingBuildingBlocks,
        Self::CraftingRedstone,
        Self::CraftingEquipment,
        Self::CraftingMisc,
        Self::FurnaceFood,
        Self::FurnaceBlocks,
        Self::FurnaceMisc,
        Self::BlastFurnaceBlocks,
        Self::BlastFurnaceMisc,
        Self::SmokerFood,
        Self::Stonecutter,
        Self::Smithing,
        Self::Campfire,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 13;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:recipe_book_category";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `minecraft:crafting_building_blocks`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `RecipeBookCategory` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for RecipeBookCategory {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(RecipeBookCategory::ALL[12].id().0 == 12);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<RecipeBookCategory>() == 1);
