// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:menu`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:menu` (25 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`Menu::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`Menu::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum Menu {
    /// `minecraft:generic_9x1` (id 0).
    Generic9x1 = 0,
    /// `minecraft:generic_9x2` (id 1).
    Generic9x2 = 1,
    /// `minecraft:generic_9x3` (id 2).
    Generic9x3 = 2,
    /// `minecraft:generic_9x4` (id 3).
    Generic9x4 = 3,
    /// `minecraft:generic_9x5` (id 4).
    Generic9x5 = 4,
    /// `minecraft:generic_9x6` (id 5).
    Generic9x6 = 5,
    /// `minecraft:generic_3x3` (id 6).
    Generic3x3 = 6,
    /// `minecraft:crafter_3x3` (id 7).
    Crafter3x3 = 7,
    /// `minecraft:anvil` (id 8).
    Anvil = 8,
    /// `minecraft:beacon` (id 9).
    Beacon = 9,
    /// `minecraft:blast_furnace` (id 10).
    BlastFurnace = 10,
    /// `minecraft:brewing_stand` (id 11).
    BrewingStand = 11,
    /// `minecraft:crafting` (id 12).
    Crafting = 12,
    /// `minecraft:enchantment` (id 13).
    Enchantment = 13,
    /// `minecraft:furnace` (id 14).
    Furnace = 14,
    /// `minecraft:grindstone` (id 15).
    Grindstone = 15,
    /// `minecraft:hopper` (id 16).
    Hopper = 16,
    /// `minecraft:lectern` (id 17).
    Lectern = 17,
    /// `minecraft:loom` (id 18).
    Loom = 18,
    /// `minecraft:merchant` (id 19).
    Merchant = 19,
    /// `minecraft:shulker_box` (id 20).
    ShulkerBox = 20,
    /// `minecraft:smithing` (id 21).
    Smithing = 21,
    /// `minecraft:smoker` (id 22).
    Smoker = 22,
    /// `minecraft:cartography_table` (id 23).
    CartographyTable = 23,
    /// `minecraft:stonecutter` (id 24).
    Stonecutter = 24,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`Menu::name`] is one load from a static.
/// `crate::generated::registry::MENU` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 25] = [
    "minecraft:generic_9x1",
    "minecraft:generic_9x2",
    "minecraft:generic_9x3",
    "minecraft:generic_9x4",
    "minecraft:generic_9x5",
    "minecraft:generic_9x6",
    "minecraft:generic_3x3",
    "minecraft:crafter_3x3",
    "minecraft:anvil",
    "minecraft:beacon",
    "minecraft:blast_furnace",
    "minecraft:brewing_stand",
    "minecraft:crafting",
    "minecraft:enchantment",
    "minecraft:furnace",
    "minecraft:grindstone",
    "minecraft:hopper",
    "minecraft:lectern",
    "minecraft:loom",
    "minecraft:merchant",
    "minecraft:shulker_box",
    "minecraft:smithing",
    "minecraft:smoker",
    "minecraft:cartography_table",
    "minecraft:stonecutter",
];

/// Discriminants ordered by name, so [`Menu::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 25 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 25] = [
    8, 9, 10, 11, 23, 7, 12, 13, 14, 6, 0, 1, 2, 3, 4, 5, 15, 16, 17, 18, 19, 20, 21, 22, 24,
];

impl Menu {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::Generic9x1,
        Self::Generic9x2,
        Self::Generic9x3,
        Self::Generic9x4,
        Self::Generic9x5,
        Self::Generic9x6,
        Self::Generic3x3,
        Self::Crafter3x3,
        Self::Anvil,
        Self::Beacon,
        Self::BlastFurnace,
        Self::BrewingStand,
        Self::Crafting,
        Self::Enchantment,
        Self::Furnace,
        Self::Grindstone,
        Self::Hopper,
        Self::Lectern,
        Self::Loom,
        Self::Merchant,
        Self::ShulkerBox,
        Self::Smithing,
        Self::Smoker,
        Self::CartographyTable,
        Self::Stonecutter,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 25;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:menu";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `minecraft:generic_9x1`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `Menu` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for Menu {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(Menu::ALL[24].id().0 == 24);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<Menu>() == 1);
