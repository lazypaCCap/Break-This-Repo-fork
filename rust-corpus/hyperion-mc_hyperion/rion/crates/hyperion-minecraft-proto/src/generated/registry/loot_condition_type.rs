// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:loot_condition_type`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:loot_condition_type` (20 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`LootConditionType::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`LootConditionType::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum LootConditionType {
    /// `minecraft:inverted` (id 0).
    Inverted = 0,
    /// `minecraft:any_of` (id 1).
    AnyOf = 1,
    /// `minecraft:all_of` (id 2).
    AllOf = 2,
    /// `minecraft:random_chance` (id 3).
    RandomChance = 3,
    /// `minecraft:random_chance_with_enchanted_bonus` (id 4).
    RandomChanceWithEnchantedBonus = 4,
    /// `minecraft:entity_properties` (id 5).
    EntityProperties = 5,
    /// `minecraft:killed_by_player` (id 6).
    KilledByPlayer = 6,
    /// `minecraft:entity_scores` (id 7).
    EntityScores = 7,
    /// `minecraft:block_state_property` (id 8).
    BlockStateProperty = 8,
    /// `minecraft:match_tool` (id 9).
    MatchTool = 9,
    /// `minecraft:table_bonus` (id 10).
    TableBonus = 10,
    /// `minecraft:survives_explosion` (id 11).
    SurvivesExplosion = 11,
    /// `minecraft:damage_source_properties` (id 12).
    DamageSourceProperties = 12,
    /// `minecraft:location_check` (id 13).
    LocationCheck = 13,
    /// `minecraft:weather_check` (id 14).
    WeatherCheck = 14,
    /// `minecraft:reference` (id 15).
    Reference = 15,
    /// `minecraft:time_check` (id 16).
    TimeCheck = 16,
    /// `minecraft:value_check` (id 17).
    ValueCheck = 17,
    /// `minecraft:enchantment_active_check` (id 18).
    EnchantmentActiveCheck = 18,
    /// `minecraft:environment_attribute_check` (id 19).
    EnvironmentAttributeCheck = 19,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`LootConditionType::name`] is one load from a static.
/// `crate::generated::registry::LOOT_CONDITION_TYPE` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 20] = [
    "minecraft:inverted",
    "minecraft:any_of",
    "minecraft:all_of",
    "minecraft:random_chance",
    "minecraft:random_chance_with_enchanted_bonus",
    "minecraft:entity_properties",
    "minecraft:killed_by_player",
    "minecraft:entity_scores",
    "minecraft:block_state_property",
    "minecraft:match_tool",
    "minecraft:table_bonus",
    "minecraft:survives_explosion",
    "minecraft:damage_source_properties",
    "minecraft:location_check",
    "minecraft:weather_check",
    "minecraft:reference",
    "minecraft:time_check",
    "minecraft:value_check",
    "minecraft:enchantment_active_check",
    "minecraft:environment_attribute_check",
];

/// Discriminants ordered by name, so [`LootConditionType::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 20 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 20] = [
    2, 1, 8, 12, 18, 5, 7, 19, 0, 6, 13, 9, 3, 4, 15, 11, 10, 16, 17, 14,
];

impl LootConditionType {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::Inverted,
        Self::AnyOf,
        Self::AllOf,
        Self::RandomChance,
        Self::RandomChanceWithEnchantedBonus,
        Self::EntityProperties,
        Self::KilledByPlayer,
        Self::EntityScores,
        Self::BlockStateProperty,
        Self::MatchTool,
        Self::TableBonus,
        Self::SurvivesExplosion,
        Self::DamageSourceProperties,
        Self::LocationCheck,
        Self::WeatherCheck,
        Self::Reference,
        Self::TimeCheck,
        Self::ValueCheck,
        Self::EnchantmentActiveCheck,
        Self::EnvironmentAttributeCheck,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 20;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:loot_condition_type";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `minecraft:inverted`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `LootConditionType` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for LootConditionType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(LootConditionType::ALL[19].id().0 == 19);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<LootConditionType>() == 1);
