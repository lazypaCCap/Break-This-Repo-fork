// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:command_argument_type`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:command_argument_type` (57 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`CommandArgumentType::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`CommandArgumentType::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum CommandArgumentType {
    /// `brigadier:bool` (id 0).
    BrigadierBool = 0,
    /// `brigadier:float` (id 1).
    BrigadierFloat = 1,
    /// `brigadier:double` (id 2).
    BrigadierDouble = 2,
    /// `brigadier:integer` (id 3).
    BrigadierInteger = 3,
    /// `brigadier:long` (id 4).
    BrigadierLong = 4,
    /// `brigadier:string` (id 5).
    BrigadierString = 5,
    /// `minecraft:entity` (id 6).
    Entity = 6,
    /// `minecraft:game_profile` (id 7).
    GameProfile = 7,
    /// `minecraft:block_pos` (id 8).
    BlockPos = 8,
    /// `minecraft:column_pos` (id 9).
    ColumnPos = 9,
    /// `minecraft:vec3` (id 10).
    Vec3 = 10,
    /// `minecraft:vec2` (id 11).
    Vec2 = 11,
    /// `minecraft:block_state` (id 12).
    BlockState = 12,
    /// `minecraft:block_predicate` (id 13).
    BlockPredicate = 13,
    /// `minecraft:item_stack` (id 14).
    ItemStack = 14,
    /// `minecraft:item_predicate` (id 15).
    ItemPredicate = 15,
    /// `minecraft:team_color` (id 16).
    TeamColor = 16,
    /// `minecraft:hex_color` (id 17).
    HexColor = 17,
    /// `minecraft:component` (id 18).
    Component = 18,
    /// `minecraft:style` (id 19).
    Style = 19,
    /// `minecraft:message` (id 20).
    Message = 20,
    /// `minecraft:nbt_compound_tag` (id 21).
    NbtCompoundTag = 21,
    /// `minecraft:nbt_tag` (id 22).
    NbtTag = 22,
    /// `minecraft:nbt_path` (id 23).
    NbtPath = 23,
    /// `minecraft:objective` (id 24).
    Objective = 24,
    /// `minecraft:objective_criteria` (id 25).
    ObjectiveCriteria = 25,
    /// `minecraft:operation` (id 26).
    Operation = 26,
    /// `minecraft:particle` (id 27).
    Particle = 27,
    /// `minecraft:angle` (id 28).
    Angle = 28,
    /// `minecraft:rotation` (id 29).
    Rotation = 29,
    /// `minecraft:scoreboard_slot` (id 30).
    ScoreboardSlot = 30,
    /// `minecraft:score_holder` (id 31).
    ScoreHolder = 31,
    /// `minecraft:swizzle` (id 32).
    Swizzle = 32,
    /// `minecraft:team` (id 33).
    Team = 33,
    /// `minecraft:item_slot` (id 34).
    ItemSlot = 34,
    /// `minecraft:item_slots` (id 35).
    ItemSlots = 35,
    /// `minecraft:resource_location` (id 36).
    ResourceLocation = 36,
    /// `minecraft:function` (id 37).
    Function = 37,
    /// `minecraft:entity_anchor` (id 38).
    EntityAnchor = 38,
    /// `minecraft:int_range` (id 39).
    IntRange = 39,
    /// `minecraft:float_range` (id 40).
    FloatRange = 40,
    /// `minecraft:dimension` (id 41).
    Dimension = 41,
    /// `minecraft:gamemode` (id 42).
    Gamemode = 42,
    /// `minecraft:time` (id 43).
    Time = 43,
    /// `minecraft:resource_or_tag` (id 44).
    ResourceOrTag = 44,
    /// `minecraft:resource_or_tag_key` (id 45).
    ResourceOrTagKey = 45,
    /// `minecraft:resource` (id 46).
    Resource = 46,
    /// `minecraft:resource_key` (id 47).
    ResourceKey = 47,
    /// `minecraft:resource_selector` (id 48).
    ResourceSelector = 48,
    /// `minecraft:template_mirror` (id 49).
    TemplateMirror = 49,
    /// `minecraft:template_rotation` (id 50).
    TemplateRotation = 50,
    /// `minecraft:heightmap` (id 51).
    Heightmap = 51,
    /// `minecraft:loot_table` (id 52).
    LootTable = 52,
    /// `minecraft:loot_predicate` (id 53).
    LootPredicate = 53,
    /// `minecraft:loot_modifier` (id 54).
    LootModifier = 54,
    /// `minecraft:dialog` (id 55).
    Dialog = 55,
    /// `minecraft:uuid` (id 56).
    Uuid = 56,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`CommandArgumentType::name`] is one load from a static.
/// `crate::generated::registry::COMMAND_ARGUMENT_TYPE` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 57] = [
    "brigadier:bool",
    "brigadier:float",
    "brigadier:double",
    "brigadier:integer",
    "brigadier:long",
    "brigadier:string",
    "minecraft:entity",
    "minecraft:game_profile",
    "minecraft:block_pos",
    "minecraft:column_pos",
    "minecraft:vec3",
    "minecraft:vec2",
    "minecraft:block_state",
    "minecraft:block_predicate",
    "minecraft:item_stack",
    "minecraft:item_predicate",
    "minecraft:team_color",
    "minecraft:hex_color",
    "minecraft:component",
    "minecraft:style",
    "minecraft:message",
    "minecraft:nbt_compound_tag",
    "minecraft:nbt_tag",
    "minecraft:nbt_path",
    "minecraft:objective",
    "minecraft:objective_criteria",
    "minecraft:operation",
    "minecraft:particle",
    "minecraft:angle",
    "minecraft:rotation",
    "minecraft:scoreboard_slot",
    "minecraft:score_holder",
    "minecraft:swizzle",
    "minecraft:team",
    "minecraft:item_slot",
    "minecraft:item_slots",
    "minecraft:resource_location",
    "minecraft:function",
    "minecraft:entity_anchor",
    "minecraft:int_range",
    "minecraft:float_range",
    "minecraft:dimension",
    "minecraft:gamemode",
    "minecraft:time",
    "minecraft:resource_or_tag",
    "minecraft:resource_or_tag_key",
    "minecraft:resource",
    "minecraft:resource_key",
    "minecraft:resource_selector",
    "minecraft:template_mirror",
    "minecraft:template_rotation",
    "minecraft:heightmap",
    "minecraft:loot_table",
    "minecraft:loot_predicate",
    "minecraft:loot_modifier",
    "minecraft:dialog",
    "minecraft:uuid",
];

/// Discriminants ordered by name, so [`CommandArgumentType::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 57 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 57] = [
    0, 2, 1, 3, 4, 5, 28, 8, 13, 12, 9, 18, 55, 41, 6, 38, 40, 37, 7, 42, 51, 17, 39, 15, 34, 35,
    14, 54, 53, 52, 20, 21, 23, 22, 24, 25, 26, 27, 46, 47, 36, 44, 45, 48, 29, 31, 30, 19, 32, 33,
    16, 49, 50, 43, 56, 11, 10,
];

impl CommandArgumentType {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::BrigadierBool,
        Self::BrigadierFloat,
        Self::BrigadierDouble,
        Self::BrigadierInteger,
        Self::BrigadierLong,
        Self::BrigadierString,
        Self::Entity,
        Self::GameProfile,
        Self::BlockPos,
        Self::ColumnPos,
        Self::Vec3,
        Self::Vec2,
        Self::BlockState,
        Self::BlockPredicate,
        Self::ItemStack,
        Self::ItemPredicate,
        Self::TeamColor,
        Self::HexColor,
        Self::Component,
        Self::Style,
        Self::Message,
        Self::NbtCompoundTag,
        Self::NbtTag,
        Self::NbtPath,
        Self::Objective,
        Self::ObjectiveCriteria,
        Self::Operation,
        Self::Particle,
        Self::Angle,
        Self::Rotation,
        Self::ScoreboardSlot,
        Self::ScoreHolder,
        Self::Swizzle,
        Self::Team,
        Self::ItemSlot,
        Self::ItemSlots,
        Self::ResourceLocation,
        Self::Function,
        Self::EntityAnchor,
        Self::IntRange,
        Self::FloatRange,
        Self::Dimension,
        Self::Gamemode,
        Self::Time,
        Self::ResourceOrTag,
        Self::ResourceOrTagKey,
        Self::Resource,
        Self::ResourceKey,
        Self::ResourceSelector,
        Self::TemplateMirror,
        Self::TemplateRotation,
        Self::Heightmap,
        Self::LootTable,
        Self::LootPredicate,
        Self::LootModifier,
        Self::Dialog,
        Self::Uuid,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 57;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:command_argument_type";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `brigadier:bool`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `CommandArgumentType` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for CommandArgumentType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(CommandArgumentType::ALL[56].id().0 == 56);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<CommandArgumentType>() == 1);
