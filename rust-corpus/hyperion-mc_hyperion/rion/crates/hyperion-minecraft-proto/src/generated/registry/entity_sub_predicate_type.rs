// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:entity_sub_predicate_type`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:entity_sub_predicate_type` (25 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`EntitySubPredicateType::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`EntitySubPredicateType::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum EntitySubPredicateType {
    /// `minecraft:entity_type` (id 0).
    EntityType = 0,
    /// `minecraft:location` (id 1).
    Location = 1,
    /// `minecraft:stepping_on` (id 2).
    SteppingOn = 2,
    /// `minecraft:movement_affected_by` (id 3).
    MovementAffectedBy = 3,
    /// `minecraft:distance` (id 4).
    Distance = 4,
    /// `minecraft:movement` (id 5).
    Movement = 5,
    /// `minecraft:effects` (id 6).
    Effects = 6,
    /// `minecraft:nbt` (id 7).
    Nbt = 7,
    /// `minecraft:flags` (id 8).
    Flags = 8,
    /// `minecraft:equipment` (id 9).
    Equipment = 9,
    /// `minecraft:periodic_tick` (id 10).
    PeriodicTick = 10,
    /// `minecraft:vehicle` (id 11).
    Vehicle = 11,
    /// `minecraft:passenger` (id 12).
    Passenger = 12,
    /// `minecraft:targeted_entity` (id 13).
    TargetedEntity = 13,
    /// `minecraft:team` (id 14).
    Team = 14,
    /// `minecraft:slots` (id 15).
    Slots = 15,
    /// `minecraft:components` (id 16).
    Components = 16,
    /// `minecraft:predicates` (id 17).
    Predicates = 17,
    /// `minecraft:entity_tags` (id 18).
    EntityTags = 18,
    /// `minecraft:type_specific/lightning` (id 19).
    TypeSpecificLightning = 19,
    /// `minecraft:type_specific/fishing_hook` (id 20).
    TypeSpecificFishingHook = 20,
    /// `minecraft:type_specific/player` (id 21).
    TypeSpecificPlayer = 21,
    /// `minecraft:type_specific/cube_mob` (id 22).
    TypeSpecificCubeMob = 22,
    /// `minecraft:type_specific/raider` (id 23).
    TypeSpecificRaider = 23,
    /// `minecraft:type_specific/sheep` (id 24).
    TypeSpecificSheep = 24,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`EntitySubPredicateType::name`] is one load from a static.
/// `crate::generated::registry::ENTITY_SUB_PREDICATE_TYPE` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 25] = [
    "minecraft:entity_type",
    "minecraft:location",
    "minecraft:stepping_on",
    "minecraft:movement_affected_by",
    "minecraft:distance",
    "minecraft:movement",
    "minecraft:effects",
    "minecraft:nbt",
    "minecraft:flags",
    "minecraft:equipment",
    "minecraft:periodic_tick",
    "minecraft:vehicle",
    "minecraft:passenger",
    "minecraft:targeted_entity",
    "minecraft:team",
    "minecraft:slots",
    "minecraft:components",
    "minecraft:predicates",
    "minecraft:entity_tags",
    "minecraft:type_specific/lightning",
    "minecraft:type_specific/fishing_hook",
    "minecraft:type_specific/player",
    "minecraft:type_specific/cube_mob",
    "minecraft:type_specific/raider",
    "minecraft:type_specific/sheep",
];

/// Discriminants ordered by name, so [`EntitySubPredicateType::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 25 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 25] = [
    16, 4, 6, 18, 0, 9, 8, 1, 5, 3, 7, 12, 10, 17, 15, 2, 13, 14, 22, 20, 19, 21, 23, 24, 11,
];

impl EntitySubPredicateType {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::EntityType,
        Self::Location,
        Self::SteppingOn,
        Self::MovementAffectedBy,
        Self::Distance,
        Self::Movement,
        Self::Effects,
        Self::Nbt,
        Self::Flags,
        Self::Equipment,
        Self::PeriodicTick,
        Self::Vehicle,
        Self::Passenger,
        Self::TargetedEntity,
        Self::Team,
        Self::Slots,
        Self::Components,
        Self::Predicates,
        Self::EntityTags,
        Self::TypeSpecificLightning,
        Self::TypeSpecificFishingHook,
        Self::TypeSpecificPlayer,
        Self::TypeSpecificCubeMob,
        Self::TypeSpecificRaider,
        Self::TypeSpecificSheep,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 25;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:entity_sub_predicate_type";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `minecraft:entity_type`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `EntitySubPredicateType` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for EntitySubPredicateType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(EntitySubPredicateType::ALL[24].id().0 == 24);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<EntitySubPredicateType>() == 1);
