// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:potion`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:potion` (46 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`Potion::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`Potion::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum Potion {
    /// `minecraft:water` (id 0).
    Water = 0,
    /// `minecraft:mundane` (id 1).
    Mundane = 1,
    /// `minecraft:thick` (id 2).
    Thick = 2,
    /// `minecraft:awkward` (id 3).
    Awkward = 3,
    /// `minecraft:night_vision` (id 4).
    NightVision = 4,
    /// `minecraft:long_night_vision` (id 5).
    LongNightVision = 5,
    /// `minecraft:invisibility` (id 6).
    Invisibility = 6,
    /// `minecraft:long_invisibility` (id 7).
    LongInvisibility = 7,
    /// `minecraft:leaping` (id 8).
    Leaping = 8,
    /// `minecraft:long_leaping` (id 9).
    LongLeaping = 9,
    /// `minecraft:strong_leaping` (id 10).
    StrongLeaping = 10,
    /// `minecraft:fire_resistance` (id 11).
    FireResistance = 11,
    /// `minecraft:long_fire_resistance` (id 12).
    LongFireResistance = 12,
    /// `minecraft:swiftness` (id 13).
    Swiftness = 13,
    /// `minecraft:long_swiftness` (id 14).
    LongSwiftness = 14,
    /// `minecraft:strong_swiftness` (id 15).
    StrongSwiftness = 15,
    /// `minecraft:slowness` (id 16).
    Slowness = 16,
    /// `minecraft:long_slowness` (id 17).
    LongSlowness = 17,
    /// `minecraft:strong_slowness` (id 18).
    StrongSlowness = 18,
    /// `minecraft:turtle_master` (id 19).
    TurtleMaster = 19,
    /// `minecraft:long_turtle_master` (id 20).
    LongTurtleMaster = 20,
    /// `minecraft:strong_turtle_master` (id 21).
    StrongTurtleMaster = 21,
    /// `minecraft:water_breathing` (id 22).
    WaterBreathing = 22,
    /// `minecraft:long_water_breathing` (id 23).
    LongWaterBreathing = 23,
    /// `minecraft:healing` (id 24).
    Healing = 24,
    /// `minecraft:strong_healing` (id 25).
    StrongHealing = 25,
    /// `minecraft:harming` (id 26).
    Harming = 26,
    /// `minecraft:strong_harming` (id 27).
    StrongHarming = 27,
    /// `minecraft:poison` (id 28).
    Poison = 28,
    /// `minecraft:long_poison` (id 29).
    LongPoison = 29,
    /// `minecraft:strong_poison` (id 30).
    StrongPoison = 30,
    /// `minecraft:regeneration` (id 31).
    Regeneration = 31,
    /// `minecraft:long_regeneration` (id 32).
    LongRegeneration = 32,
    /// `minecraft:strong_regeneration` (id 33).
    StrongRegeneration = 33,
    /// `minecraft:strength` (id 34).
    Strength = 34,
    /// `minecraft:long_strength` (id 35).
    LongStrength = 35,
    /// `minecraft:strong_strength` (id 36).
    StrongStrength = 36,
    /// `minecraft:weakness` (id 37).
    Weakness = 37,
    /// `minecraft:long_weakness` (id 38).
    LongWeakness = 38,
    /// `minecraft:luck` (id 39).
    Luck = 39,
    /// `minecraft:slow_falling` (id 40).
    SlowFalling = 40,
    /// `minecraft:long_slow_falling` (id 41).
    LongSlowFalling = 41,
    /// `minecraft:wind_charged` (id 42).
    WindCharged = 42,
    /// `minecraft:weaving` (id 43).
    Weaving = 43,
    /// `minecraft:oozing` (id 44).
    Oozing = 44,
    /// `minecraft:infested` (id 45).
    Infested = 45,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`Potion::name`] is one load from a static.
/// `crate::generated::registry::POTION` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 46] = [
    "minecraft:water",
    "minecraft:mundane",
    "minecraft:thick",
    "minecraft:awkward",
    "minecraft:night_vision",
    "minecraft:long_night_vision",
    "minecraft:invisibility",
    "minecraft:long_invisibility",
    "minecraft:leaping",
    "minecraft:long_leaping",
    "minecraft:strong_leaping",
    "minecraft:fire_resistance",
    "minecraft:long_fire_resistance",
    "minecraft:swiftness",
    "minecraft:long_swiftness",
    "minecraft:strong_swiftness",
    "minecraft:slowness",
    "minecraft:long_slowness",
    "minecraft:strong_slowness",
    "minecraft:turtle_master",
    "minecraft:long_turtle_master",
    "minecraft:strong_turtle_master",
    "minecraft:water_breathing",
    "minecraft:long_water_breathing",
    "minecraft:healing",
    "minecraft:strong_healing",
    "minecraft:harming",
    "minecraft:strong_harming",
    "minecraft:poison",
    "minecraft:long_poison",
    "minecraft:strong_poison",
    "minecraft:regeneration",
    "minecraft:long_regeneration",
    "minecraft:strong_regeneration",
    "minecraft:strength",
    "minecraft:long_strength",
    "minecraft:strong_strength",
    "minecraft:weakness",
    "minecraft:long_weakness",
    "minecraft:luck",
    "minecraft:slow_falling",
    "minecraft:long_slow_falling",
    "minecraft:wind_charged",
    "minecraft:weaving",
    "minecraft:oozing",
    "minecraft:infested",
];

/// Discriminants ordered by name, so [`Potion::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 46 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 46] = [
    3, 11, 26, 24, 45, 6, 8, 12, 7, 9, 5, 29, 32, 41, 17, 35, 14, 20, 23, 38, 39, 1, 4, 44, 28, 31,
    40, 16, 34, 27, 25, 10, 30, 33, 18, 36, 15, 21, 13, 2, 19, 0, 22, 37, 43, 42,
];

impl Potion {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::Water,
        Self::Mundane,
        Self::Thick,
        Self::Awkward,
        Self::NightVision,
        Self::LongNightVision,
        Self::Invisibility,
        Self::LongInvisibility,
        Self::Leaping,
        Self::LongLeaping,
        Self::StrongLeaping,
        Self::FireResistance,
        Self::LongFireResistance,
        Self::Swiftness,
        Self::LongSwiftness,
        Self::StrongSwiftness,
        Self::Slowness,
        Self::LongSlowness,
        Self::StrongSlowness,
        Self::TurtleMaster,
        Self::LongTurtleMaster,
        Self::StrongTurtleMaster,
        Self::WaterBreathing,
        Self::LongWaterBreathing,
        Self::Healing,
        Self::StrongHealing,
        Self::Harming,
        Self::StrongHarming,
        Self::Poison,
        Self::LongPoison,
        Self::StrongPoison,
        Self::Regeneration,
        Self::LongRegeneration,
        Self::StrongRegeneration,
        Self::Strength,
        Self::LongStrength,
        Self::StrongStrength,
        Self::Weakness,
        Self::LongWeakness,
        Self::Luck,
        Self::SlowFalling,
        Self::LongSlowFalling,
        Self::WindCharged,
        Self::Weaving,
        Self::Oozing,
        Self::Infested,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 46;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:potion";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `minecraft:water`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `Potion` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for Potion {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(Potion::ALL[45].id().0 == 45);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<Potion>() == 1);
