// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:mob_effect`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:mob_effect` (40 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`MobEffect::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`MobEffect::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum MobEffect {
    /// `minecraft:speed` (id 0).
    Speed = 0,
    /// `minecraft:slowness` (id 1).
    Slowness = 1,
    /// `minecraft:haste` (id 2).
    Haste = 2,
    /// `minecraft:mining_fatigue` (id 3).
    MiningFatigue = 3,
    /// `minecraft:strength` (id 4).
    Strength = 4,
    /// `minecraft:instant_health` (id 5).
    InstantHealth = 5,
    /// `minecraft:instant_damage` (id 6).
    InstantDamage = 6,
    /// `minecraft:jump_boost` (id 7).
    JumpBoost = 7,
    /// `minecraft:nausea` (id 8).
    Nausea = 8,
    /// `minecraft:regeneration` (id 9).
    Regeneration = 9,
    /// `minecraft:resistance` (id 10).
    Resistance = 10,
    /// `minecraft:fire_resistance` (id 11).
    FireResistance = 11,
    /// `minecraft:water_breathing` (id 12).
    WaterBreathing = 12,
    /// `minecraft:invisibility` (id 13).
    Invisibility = 13,
    /// `minecraft:blindness` (id 14).
    Blindness = 14,
    /// `minecraft:night_vision` (id 15).
    NightVision = 15,
    /// `minecraft:hunger` (id 16).
    Hunger = 16,
    /// `minecraft:weakness` (id 17).
    Weakness = 17,
    /// `minecraft:poison` (id 18).
    Poison = 18,
    /// `minecraft:wither` (id 19).
    Wither = 19,
    /// `minecraft:health_boost` (id 20).
    HealthBoost = 20,
    /// `minecraft:absorption` (id 21).
    Absorption = 21,
    /// `minecraft:saturation` (id 22).
    Saturation = 22,
    /// `minecraft:glowing` (id 23).
    Glowing = 23,
    /// `minecraft:levitation` (id 24).
    Levitation = 24,
    /// `minecraft:luck` (id 25).
    Luck = 25,
    /// `minecraft:unluck` (id 26).
    Unluck = 26,
    /// `minecraft:slow_falling` (id 27).
    SlowFalling = 27,
    /// `minecraft:conduit_power` (id 28).
    ConduitPower = 28,
    /// `minecraft:dolphins_grace` (id 29).
    DolphinsGrace = 29,
    /// `minecraft:bad_omen` (id 30).
    BadOmen = 30,
    /// `minecraft:hero_of_the_village` (id 31).
    HeroOfTheVillage = 31,
    /// `minecraft:darkness` (id 32).
    Darkness = 32,
    /// `minecraft:trial_omen` (id 33).
    TrialOmen = 33,
    /// `minecraft:raid_omen` (id 34).
    RaidOmen = 34,
    /// `minecraft:wind_charged` (id 35).
    WindCharged = 35,
    /// `minecraft:weaving` (id 36).
    Weaving = 36,
    /// `minecraft:oozing` (id 37).
    Oozing = 37,
    /// `minecraft:infested` (id 38).
    Infested = 38,
    /// `minecraft:breath_of_the_nautilus` (id 39).
    BreathOfTheNautilus = 39,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`MobEffect::name`] is one load from a static.
/// `crate::generated::registry::MOB_EFFECT` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 40] = [
    "minecraft:speed",
    "minecraft:slowness",
    "minecraft:haste",
    "minecraft:mining_fatigue",
    "minecraft:strength",
    "minecraft:instant_health",
    "minecraft:instant_damage",
    "minecraft:jump_boost",
    "minecraft:nausea",
    "minecraft:regeneration",
    "minecraft:resistance",
    "minecraft:fire_resistance",
    "minecraft:water_breathing",
    "minecraft:invisibility",
    "minecraft:blindness",
    "minecraft:night_vision",
    "minecraft:hunger",
    "minecraft:weakness",
    "minecraft:poison",
    "minecraft:wither",
    "minecraft:health_boost",
    "minecraft:absorption",
    "minecraft:saturation",
    "minecraft:glowing",
    "minecraft:levitation",
    "minecraft:luck",
    "minecraft:unluck",
    "minecraft:slow_falling",
    "minecraft:conduit_power",
    "minecraft:dolphins_grace",
    "minecraft:bad_omen",
    "minecraft:hero_of_the_village",
    "minecraft:darkness",
    "minecraft:trial_omen",
    "minecraft:raid_omen",
    "minecraft:wind_charged",
    "minecraft:weaving",
    "minecraft:oozing",
    "minecraft:infested",
    "minecraft:breath_of_the_nautilus",
];

/// Discriminants ordered by name, so [`MobEffect::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 40 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 40] = [
    21, 30, 14, 39, 28, 32, 29, 11, 23, 2, 20, 31, 16, 38, 6, 5, 13, 7, 24, 25, 3, 8, 15, 37, 18,
    34, 9, 10, 22, 27, 1, 0, 4, 33, 26, 12, 17, 36, 35, 19,
];

impl MobEffect {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::Speed,
        Self::Slowness,
        Self::Haste,
        Self::MiningFatigue,
        Self::Strength,
        Self::InstantHealth,
        Self::InstantDamage,
        Self::JumpBoost,
        Self::Nausea,
        Self::Regeneration,
        Self::Resistance,
        Self::FireResistance,
        Self::WaterBreathing,
        Self::Invisibility,
        Self::Blindness,
        Self::NightVision,
        Self::Hunger,
        Self::Weakness,
        Self::Poison,
        Self::Wither,
        Self::HealthBoost,
        Self::Absorption,
        Self::Saturation,
        Self::Glowing,
        Self::Levitation,
        Self::Luck,
        Self::Unluck,
        Self::SlowFalling,
        Self::ConduitPower,
        Self::DolphinsGrace,
        Self::BadOmen,
        Self::HeroOfTheVillage,
        Self::Darkness,
        Self::TrialOmen,
        Self::RaidOmen,
        Self::WindCharged,
        Self::Weaving,
        Self::Oozing,
        Self::Infested,
        Self::BreathOfTheNautilus,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 40;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:mob_effect";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `minecraft:speed`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `MobEffect` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for MobEffect {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(MobEffect::ALL[39].id().0 == 39);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<MobEffect>() == 1);
