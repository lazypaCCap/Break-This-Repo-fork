// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:loot_function_type`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:loot_function_type` (43 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`LootFunctionType::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`LootFunctionType::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum LootFunctionType {
    /// `minecraft:set_count` (id 0).
    SetCount = 0,
    /// `minecraft:set_item` (id 1).
    SetItem = 1,
    /// `minecraft:enchant_with_levels` (id 2).
    EnchantWithLevels = 2,
    /// `minecraft:enchant_randomly` (id 3).
    EnchantRandomly = 3,
    /// `minecraft:set_enchantments` (id 4).
    SetEnchantments = 4,
    /// `minecraft:set_custom_data` (id 5).
    SetCustomData = 5,
    /// `minecraft:set_components` (id 6).
    SetComponents = 6,
    /// `minecraft:furnace_smelt` (id 7).
    FurnaceSmelt = 7,
    /// `minecraft:enchanted_count_increase` (id 8).
    EnchantedCountIncrease = 8,
    /// `minecraft:set_damage` (id 9).
    SetDamage = 9,
    /// `minecraft:set_attributes` (id 10).
    SetAttributes = 10,
    /// `minecraft:set_name` (id 11).
    SetName = 11,
    /// `minecraft:exploration_map` (id 12).
    ExplorationMap = 12,
    /// `minecraft:set_stew_effect` (id 13).
    SetStewEffect = 13,
    /// `minecraft:copy_name` (id 14).
    CopyName = 14,
    /// `minecraft:set_contents` (id 15).
    SetContents = 15,
    /// `minecraft:modify_contents` (id 16).
    ModifyContents = 16,
    /// `minecraft:filtered` (id 17).
    Filtered = 17,
    /// `minecraft:limit_count` (id 18).
    LimitCount = 18,
    /// `minecraft:apply_bonus` (id 19).
    ApplyBonus = 19,
    /// `minecraft:set_loot_table` (id 20).
    SetLootTable = 20,
    /// `minecraft:explosion_decay` (id 21).
    ExplosionDecay = 21,
    /// `minecraft:set_lore` (id 22).
    SetLore = 22,
    /// `minecraft:fill_player_head` (id 23).
    FillPlayerHead = 23,
    /// `minecraft:copy_custom_data` (id 24).
    CopyCustomData = 24,
    /// `minecraft:copy_state` (id 25).
    CopyState = 25,
    /// `minecraft:set_banner_pattern` (id 26).
    SetBannerPattern = 26,
    /// `minecraft:set_potion` (id 27).
    SetPotion = 27,
    /// `minecraft:set_random_dyes` (id 28).
    SetRandomDyes = 28,
    /// `minecraft:set_random_potion` (id 29).
    SetRandomPotion = 29,
    /// `minecraft:set_instrument` (id 30).
    SetInstrument = 30,
    /// `minecraft:reference` (id 31).
    Reference = 31,
    /// `minecraft:sequence` (id 32).
    Sequence = 32,
    /// `minecraft:copy_components` (id 33).
    CopyComponents = 33,
    /// `minecraft:set_fireworks` (id 34).
    SetFireworks = 34,
    /// `minecraft:set_firework_explosion` (id 35).
    SetFireworkExplosion = 35,
    /// `minecraft:set_book_cover` (id 36).
    SetBookCover = 36,
    /// `minecraft:set_written_book_pages` (id 37).
    SetWrittenBookPages = 37,
    /// `minecraft:set_writable_book_pages` (id 38).
    SetWritableBookPages = 38,
    /// `minecraft:toggle_tooltips` (id 39).
    ToggleTooltips = 39,
    /// `minecraft:set_ominous_bottle_amplifier` (id 40).
    SetOminousBottleAmplifier = 40,
    /// `minecraft:set_custom_model_data` (id 41).
    SetCustomModelData = 41,
    /// `minecraft:discard` (id 42).
    Discard = 42,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`LootFunctionType::name`] is one load from a static.
/// `crate::generated::registry::LOOT_FUNCTION_TYPE` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 43] = [
    "minecraft:set_count",
    "minecraft:set_item",
    "minecraft:enchant_with_levels",
    "minecraft:enchant_randomly",
    "minecraft:set_enchantments",
    "minecraft:set_custom_data",
    "minecraft:set_components",
    "minecraft:furnace_smelt",
    "minecraft:enchanted_count_increase",
    "minecraft:set_damage",
    "minecraft:set_attributes",
    "minecraft:set_name",
    "minecraft:exploration_map",
    "minecraft:set_stew_effect",
    "minecraft:copy_name",
    "minecraft:set_contents",
    "minecraft:modify_contents",
    "minecraft:filtered",
    "minecraft:limit_count",
    "minecraft:apply_bonus",
    "minecraft:set_loot_table",
    "minecraft:explosion_decay",
    "minecraft:set_lore",
    "minecraft:fill_player_head",
    "minecraft:copy_custom_data",
    "minecraft:copy_state",
    "minecraft:set_banner_pattern",
    "minecraft:set_potion",
    "minecraft:set_random_dyes",
    "minecraft:set_random_potion",
    "minecraft:set_instrument",
    "minecraft:reference",
    "minecraft:sequence",
    "minecraft:copy_components",
    "minecraft:set_fireworks",
    "minecraft:set_firework_explosion",
    "minecraft:set_book_cover",
    "minecraft:set_written_book_pages",
    "minecraft:set_writable_book_pages",
    "minecraft:toggle_tooltips",
    "minecraft:set_ominous_bottle_amplifier",
    "minecraft:set_custom_model_data",
    "minecraft:discard",
];

/// Discriminants ordered by name, so [`LootFunctionType::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 43 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 43] = [
    19, 33, 24, 14, 25, 42, 3, 2, 8, 12, 21, 23, 17, 7, 18, 16, 31, 32, 10, 26, 36, 6, 15, 0, 5,
    41, 9, 4, 35, 34, 30, 1, 20, 22, 11, 40, 27, 28, 29, 13, 38, 37, 39,
];

impl LootFunctionType {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::SetCount,
        Self::SetItem,
        Self::EnchantWithLevels,
        Self::EnchantRandomly,
        Self::SetEnchantments,
        Self::SetCustomData,
        Self::SetComponents,
        Self::FurnaceSmelt,
        Self::EnchantedCountIncrease,
        Self::SetDamage,
        Self::SetAttributes,
        Self::SetName,
        Self::ExplorationMap,
        Self::SetStewEffect,
        Self::CopyName,
        Self::SetContents,
        Self::ModifyContents,
        Self::Filtered,
        Self::LimitCount,
        Self::ApplyBonus,
        Self::SetLootTable,
        Self::ExplosionDecay,
        Self::SetLore,
        Self::FillPlayerHead,
        Self::CopyCustomData,
        Self::CopyState,
        Self::SetBannerPattern,
        Self::SetPotion,
        Self::SetRandomDyes,
        Self::SetRandomPotion,
        Self::SetInstrument,
        Self::Reference,
        Self::Sequence,
        Self::CopyComponents,
        Self::SetFireworks,
        Self::SetFireworkExplosion,
        Self::SetBookCover,
        Self::SetWrittenBookPages,
        Self::SetWritableBookPages,
        Self::ToggleTooltips,
        Self::SetOminousBottleAmplifier,
        Self::SetCustomModelData,
        Self::Discard,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 43;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:loot_function_type";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `minecraft:set_count`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `LootFunctionType` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for LootFunctionType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(LootFunctionType::ALL[42].id().0 == 42);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<LootFunctionType>() == 1);
