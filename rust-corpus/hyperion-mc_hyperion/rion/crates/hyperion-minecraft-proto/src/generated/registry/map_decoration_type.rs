// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:map_decoration_type`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:map_decoration_type` (35 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`MapDecorationType::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`MapDecorationType::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum MapDecorationType {
    /// `minecraft:player` (id 0).
    Player = 0,
    /// `minecraft:frame` (id 1).
    Frame = 1,
    /// `minecraft:red_marker` (id 2).
    RedMarker = 2,
    /// `minecraft:blue_marker` (id 3).
    BlueMarker = 3,
    /// `minecraft:target_x` (id 4).
    TargetX = 4,
    /// `minecraft:target_point` (id 5).
    TargetPoint = 5,
    /// `minecraft:player_off_map` (id 6).
    PlayerOffMap = 6,
    /// `minecraft:player_off_limits` (id 7).
    PlayerOffLimits = 7,
    /// `minecraft:mansion` (id 8).
    Mansion = 8,
    /// `minecraft:monument` (id 9).
    Monument = 9,
    /// `minecraft:banner_white` (id 10).
    BannerWhite = 10,
    /// `minecraft:banner_orange` (id 11).
    BannerOrange = 11,
    /// `minecraft:banner_magenta` (id 12).
    BannerMagenta = 12,
    /// `minecraft:banner_light_blue` (id 13).
    BannerLightBlue = 13,
    /// `minecraft:banner_yellow` (id 14).
    BannerYellow = 14,
    /// `minecraft:banner_lime` (id 15).
    BannerLime = 15,
    /// `minecraft:banner_pink` (id 16).
    BannerPink = 16,
    /// `minecraft:banner_gray` (id 17).
    BannerGray = 17,
    /// `minecraft:banner_light_gray` (id 18).
    BannerLightGray = 18,
    /// `minecraft:banner_cyan` (id 19).
    BannerCyan = 19,
    /// `minecraft:banner_purple` (id 20).
    BannerPurple = 20,
    /// `minecraft:banner_blue` (id 21).
    BannerBlue = 21,
    /// `minecraft:banner_brown` (id 22).
    BannerBrown = 22,
    /// `minecraft:banner_green` (id 23).
    BannerGreen = 23,
    /// `minecraft:banner_red` (id 24).
    BannerRed = 24,
    /// `minecraft:banner_black` (id 25).
    BannerBlack = 25,
    /// `minecraft:red_x` (id 26).
    RedX = 26,
    /// `minecraft:village_desert` (id 27).
    VillageDesert = 27,
    /// `minecraft:village_plains` (id 28).
    VillagePlains = 28,
    /// `minecraft:village_savanna` (id 29).
    VillageSavanna = 29,
    /// `minecraft:village_snowy` (id 30).
    VillageSnowy = 30,
    /// `minecraft:village_taiga` (id 31).
    VillageTaiga = 31,
    /// `minecraft:jungle_temple` (id 32).
    JungleTemple = 32,
    /// `minecraft:swamp_hut` (id 33).
    SwampHut = 33,
    /// `minecraft:trial_chambers` (id 34).
    TrialChambers = 34,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`MapDecorationType::name`] is one load from a static.
/// `crate::generated::registry::MAP_DECORATION_TYPE` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 35] = [
    "minecraft:player",
    "minecraft:frame",
    "minecraft:red_marker",
    "minecraft:blue_marker",
    "minecraft:target_x",
    "minecraft:target_point",
    "minecraft:player_off_map",
    "minecraft:player_off_limits",
    "minecraft:mansion",
    "minecraft:monument",
    "minecraft:banner_white",
    "minecraft:banner_orange",
    "minecraft:banner_magenta",
    "minecraft:banner_light_blue",
    "minecraft:banner_yellow",
    "minecraft:banner_lime",
    "minecraft:banner_pink",
    "minecraft:banner_gray",
    "minecraft:banner_light_gray",
    "minecraft:banner_cyan",
    "minecraft:banner_purple",
    "minecraft:banner_blue",
    "minecraft:banner_brown",
    "minecraft:banner_green",
    "minecraft:banner_red",
    "minecraft:banner_black",
    "minecraft:red_x",
    "minecraft:village_desert",
    "minecraft:village_plains",
    "minecraft:village_savanna",
    "minecraft:village_snowy",
    "minecraft:village_taiga",
    "minecraft:jungle_temple",
    "minecraft:swamp_hut",
    "minecraft:trial_chambers",
];

/// Discriminants ordered by name, so [`MapDecorationType::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 35 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 35] = [
    25, 21, 22, 19, 17, 23, 13, 18, 15, 12, 11, 16, 20, 24, 10, 14, 3, 1, 32, 8, 9, 0, 7, 6, 2, 26,
    33, 5, 4, 34, 27, 28, 29, 30, 31,
];

impl MapDecorationType {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::Player,
        Self::Frame,
        Self::RedMarker,
        Self::BlueMarker,
        Self::TargetX,
        Self::TargetPoint,
        Self::PlayerOffMap,
        Self::PlayerOffLimits,
        Self::Mansion,
        Self::Monument,
        Self::BannerWhite,
        Self::BannerOrange,
        Self::BannerMagenta,
        Self::BannerLightBlue,
        Self::BannerYellow,
        Self::BannerLime,
        Self::BannerPink,
        Self::BannerGray,
        Self::BannerLightGray,
        Self::BannerCyan,
        Self::BannerPurple,
        Self::BannerBlue,
        Self::BannerBrown,
        Self::BannerGreen,
        Self::BannerRed,
        Self::BannerBlack,
        Self::RedX,
        Self::VillageDesert,
        Self::VillagePlains,
        Self::VillageSavanna,
        Self::VillageSnowy,
        Self::VillageTaiga,
        Self::JungleTemple,
        Self::SwampHut,
        Self::TrialChambers,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 35;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:map_decoration_type";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `minecraft:player`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `MapDecorationType` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for MapDecorationType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(MapDecorationType::ALL[34].id().0 == 34);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<MapDecorationType>() == 1);
