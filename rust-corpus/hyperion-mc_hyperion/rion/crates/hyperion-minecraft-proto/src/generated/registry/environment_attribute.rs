// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:environment_attribute`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:environment_attribute` (48 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`EnvironmentAttribute::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`EnvironmentAttribute::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum EnvironmentAttribute {
    /// `minecraft:visual/fog_color` (id 0).
    VisualFogColor = 0,
    /// `minecraft:visual/fog_start_distance` (id 1).
    VisualFogStartDistance = 1,
    /// `minecraft:visual/fog_end_distance` (id 2).
    VisualFogEndDistance = 2,
    /// `minecraft:visual/sky_fog_end_distance` (id 3).
    VisualSkyFogEndDistance = 3,
    /// `minecraft:visual/cloud_fog_end_distance` (id 4).
    VisualCloudFogEndDistance = 4,
    /// `minecraft:visual/water_fog_color` (id 5).
    VisualWaterFogColor = 5,
    /// `minecraft:visual/water_fog_start_distance` (id 6).
    VisualWaterFogStartDistance = 6,
    /// `minecraft:visual/water_fog_end_distance` (id 7).
    VisualWaterFogEndDistance = 7,
    /// `minecraft:visual/sky_color` (id 8).
    VisualSkyColor = 8,
    /// `minecraft:visual/sunrise_sunset_color` (id 9).
    VisualSunriseSunsetColor = 9,
    /// `minecraft:visual/cloud_color` (id 10).
    VisualCloudColor = 10,
    /// `minecraft:visual/cloud_height` (id 11).
    VisualCloudHeight = 11,
    /// `minecraft:visual/sun_angle` (id 12).
    VisualSunAngle = 12,
    /// `minecraft:visual/moon_angle` (id 13).
    VisualMoonAngle = 13,
    /// `minecraft:visual/star_angle` (id 14).
    VisualStarAngle = 14,
    /// `minecraft:visual/moon_phase` (id 15).
    VisualMoonPhase = 15,
    /// `minecraft:visual/star_brightness` (id 16).
    VisualStarBrightness = 16,
    /// `minecraft:visual/block_light_tint` (id 17).
    VisualBlockLightTint = 17,
    /// `minecraft:visual/sky_light_color` (id 18).
    VisualSkyLightColor = 18,
    /// `minecraft:visual/sky_light_factor` (id 19).
    VisualSkyLightFactor = 19,
    /// `minecraft:visual/night_vision_color` (id 20).
    VisualNightVisionColor = 20,
    /// `minecraft:visual/ambient_light_color` (id 21).
    VisualAmbientLightColor = 21,
    /// `minecraft:visual/default_dripstone_particle` (id 22).
    VisualDefaultDripstoneParticle = 22,
    /// `minecraft:visual/ambient_particles` (id 23).
    VisualAmbientParticles = 23,
    /// `minecraft:audio/background_music` (id 24).
    AudioBackgroundMusic = 24,
    /// `minecraft:audio/music_volume` (id 25).
    AudioMusicVolume = 25,
    /// `minecraft:audio/ambient_sounds` (id 26).
    AudioAmbientSounds = 26,
    /// `minecraft:audio/firefly_bush_sounds` (id 27).
    AudioFireflyBushSounds = 27,
    /// `minecraft:gameplay/sky_light_level` (id 28).
    GameplaySkyLightLevel = 28,
    /// `minecraft:gameplay/can_start_raid` (id 29).
    GameplayCanStartRaid = 29,
    /// `minecraft:gameplay/water_evaporates` (id 30).
    GameplayWaterEvaporates = 30,
    /// `minecraft:gameplay/bed_rule` (id 31).
    GameplayBedRule = 31,
    /// `minecraft:gameplay/respawn_anchor_works` (id 32).
    GameplayRespawnAnchorWorks = 32,
    /// `minecraft:gameplay/nether_portal_spawns_piglin` (id 33).
    GameplayNetherPortalSpawnsPiglin = 33,
    /// `minecraft:gameplay/fast_lava` (id 34).
    GameplayFastLava = 34,
    /// `minecraft:gameplay/increased_fire_burnout` (id 35).
    GameplayIncreasedFireBurnout = 35,
    /// `minecraft:gameplay/eyeblossom_open` (id 36).
    GameplayEyeblossomOpen = 36,
    /// `minecraft:gameplay/turtle_egg_hatch_chance` (id 37).
    GameplayTurtleEggHatchChance = 37,
    /// `minecraft:gameplay/piglins_zombify` (id 38).
    GameplayPiglinsZombify = 38,
    /// `minecraft:gameplay/snow_golem_melts` (id 39).
    GameplaySnowGolemMelts = 39,
    /// `minecraft:gameplay/creaking_active` (id 40).
    GameplayCreakingActive = 40,
    /// `minecraft:gameplay/surface_slime_spawn_chance` (id 41).
    GameplaySurfaceSlimeSpawnChance = 41,
    /// `minecraft:gameplay/cat_waking_up_gift_chance` (id 42).
    GameplayCatWakingUpGiftChance = 42,
    /// `minecraft:gameplay/bees_stay_in_hive` (id 43).
    GameplayBeesStayInHive = 43,
    /// `minecraft:gameplay/monsters_burn` (id 44).
    GameplayMonstersBurn = 44,
    /// `minecraft:gameplay/can_pillager_patrol_spawn` (id 45).
    GameplayCanPillagerPatrolSpawn = 45,
    /// `minecraft:gameplay/villager_activity` (id 46).
    GameplayVillagerActivity = 46,
    /// `minecraft:gameplay/baby_villager_activity` (id 47).
    GameplayBabyVillagerActivity = 47,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`EnvironmentAttribute::name`] is one load from a static.
/// `crate::generated::registry::ENVIRONMENT_ATTRIBUTE` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 48] = [
    "minecraft:visual/fog_color",
    "minecraft:visual/fog_start_distance",
    "minecraft:visual/fog_end_distance",
    "minecraft:visual/sky_fog_end_distance",
    "minecraft:visual/cloud_fog_end_distance",
    "minecraft:visual/water_fog_color",
    "minecraft:visual/water_fog_start_distance",
    "minecraft:visual/water_fog_end_distance",
    "minecraft:visual/sky_color",
    "minecraft:visual/sunrise_sunset_color",
    "minecraft:visual/cloud_color",
    "minecraft:visual/cloud_height",
    "minecraft:visual/sun_angle",
    "minecraft:visual/moon_angle",
    "minecraft:visual/star_angle",
    "minecraft:visual/moon_phase",
    "minecraft:visual/star_brightness",
    "minecraft:visual/block_light_tint",
    "minecraft:visual/sky_light_color",
    "minecraft:visual/sky_light_factor",
    "minecraft:visual/night_vision_color",
    "minecraft:visual/ambient_light_color",
    "minecraft:visual/default_dripstone_particle",
    "minecraft:visual/ambient_particles",
    "minecraft:audio/background_music",
    "minecraft:audio/music_volume",
    "minecraft:audio/ambient_sounds",
    "minecraft:audio/firefly_bush_sounds",
    "minecraft:gameplay/sky_light_level",
    "minecraft:gameplay/can_start_raid",
    "minecraft:gameplay/water_evaporates",
    "minecraft:gameplay/bed_rule",
    "minecraft:gameplay/respawn_anchor_works",
    "minecraft:gameplay/nether_portal_spawns_piglin",
    "minecraft:gameplay/fast_lava",
    "minecraft:gameplay/increased_fire_burnout",
    "minecraft:gameplay/eyeblossom_open",
    "minecraft:gameplay/turtle_egg_hatch_chance",
    "minecraft:gameplay/piglins_zombify",
    "minecraft:gameplay/snow_golem_melts",
    "minecraft:gameplay/creaking_active",
    "minecraft:gameplay/surface_slime_spawn_chance",
    "minecraft:gameplay/cat_waking_up_gift_chance",
    "minecraft:gameplay/bees_stay_in_hive",
    "minecraft:gameplay/monsters_burn",
    "minecraft:gameplay/can_pillager_patrol_spawn",
    "minecraft:gameplay/villager_activity",
    "minecraft:gameplay/baby_villager_activity",
];

/// Discriminants ordered by name, so [`EnvironmentAttribute::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 48 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 48] = [
    26, 24, 27, 25, 47, 31, 43, 45, 29, 42, 40, 36, 34, 35, 44, 33, 38, 32, 28, 39, 41, 37, 46, 30,
    21, 23, 17, 10, 4, 11, 22, 0, 2, 1, 13, 15, 20, 8, 3, 18, 19, 14, 16, 12, 9, 5, 7, 6,
];

impl EnvironmentAttribute {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::VisualFogColor,
        Self::VisualFogStartDistance,
        Self::VisualFogEndDistance,
        Self::VisualSkyFogEndDistance,
        Self::VisualCloudFogEndDistance,
        Self::VisualWaterFogColor,
        Self::VisualWaterFogStartDistance,
        Self::VisualWaterFogEndDistance,
        Self::VisualSkyColor,
        Self::VisualSunriseSunsetColor,
        Self::VisualCloudColor,
        Self::VisualCloudHeight,
        Self::VisualSunAngle,
        Self::VisualMoonAngle,
        Self::VisualStarAngle,
        Self::VisualMoonPhase,
        Self::VisualStarBrightness,
        Self::VisualBlockLightTint,
        Self::VisualSkyLightColor,
        Self::VisualSkyLightFactor,
        Self::VisualNightVisionColor,
        Self::VisualAmbientLightColor,
        Self::VisualDefaultDripstoneParticle,
        Self::VisualAmbientParticles,
        Self::AudioBackgroundMusic,
        Self::AudioMusicVolume,
        Self::AudioAmbientSounds,
        Self::AudioFireflyBushSounds,
        Self::GameplaySkyLightLevel,
        Self::GameplayCanStartRaid,
        Self::GameplayWaterEvaporates,
        Self::GameplayBedRule,
        Self::GameplayRespawnAnchorWorks,
        Self::GameplayNetherPortalSpawnsPiglin,
        Self::GameplayFastLava,
        Self::GameplayIncreasedFireBurnout,
        Self::GameplayEyeblossomOpen,
        Self::GameplayTurtleEggHatchChance,
        Self::GameplayPiglinsZombify,
        Self::GameplaySnowGolemMelts,
        Self::GameplayCreakingActive,
        Self::GameplaySurfaceSlimeSpawnChance,
        Self::GameplayCatWakingUpGiftChance,
        Self::GameplayBeesStayInHive,
        Self::GameplayMonstersBurn,
        Self::GameplayCanPillagerPatrolSpawn,
        Self::GameplayVillagerActivity,
        Self::GameplayBabyVillagerActivity,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 48;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:environment_attribute";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `minecraft:visual/fog_color`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `EnvironmentAttribute` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for EnvironmentAttribute {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(EnvironmentAttribute::ALL[47].id().0 == 47);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<EnvironmentAttribute>() == 1);
