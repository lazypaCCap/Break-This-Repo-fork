// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:block_entity_type`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:block_entity_type` (49 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`BlockEntityType::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`BlockEntityType::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum BlockEntityType {
    /// `minecraft:furnace` (id 0).
    Furnace = 0,
    /// `minecraft:chest` (id 1).
    Chest = 1,
    /// `minecraft:trapped_chest` (id 2).
    TrappedChest = 2,
    /// `minecraft:ender_chest` (id 3).
    EnderChest = 3,
    /// `minecraft:jukebox` (id 4).
    Jukebox = 4,
    /// `minecraft:dispenser` (id 5).
    Dispenser = 5,
    /// `minecraft:dropper` (id 6).
    Dropper = 6,
    /// `minecraft:sign` (id 7).
    Sign = 7,
    /// `minecraft:hanging_sign` (id 8).
    HangingSign = 8,
    /// `minecraft:mob_spawner` (id 9).
    MobSpawner = 9,
    /// `minecraft:creaking_heart` (id 10).
    CreakingHeart = 10,
    /// `minecraft:piston` (id 11).
    Piston = 11,
    /// `minecraft:brewing_stand` (id 12).
    BrewingStand = 12,
    /// `minecraft:enchanting_table` (id 13).
    EnchantingTable = 13,
    /// `minecraft:end_portal` (id 14).
    EndPortal = 14,
    /// `minecraft:beacon` (id 15).
    Beacon = 15,
    /// `minecraft:skull` (id 16).
    Skull = 16,
    /// `minecraft:daylight_detector` (id 17).
    DaylightDetector = 17,
    /// `minecraft:hopper` (id 18).
    Hopper = 18,
    /// `minecraft:comparator` (id 19).
    Comparator = 19,
    /// `minecraft:banner` (id 20).
    Banner = 20,
    /// `minecraft:structure_block` (id 21).
    StructureBlock = 21,
    /// `minecraft:end_gateway` (id 22).
    EndGateway = 22,
    /// `minecraft:command_block` (id 23).
    CommandBlock = 23,
    /// `minecraft:shulker_box` (id 24).
    ShulkerBox = 24,
    /// `minecraft:conduit` (id 25).
    Conduit = 25,
    /// `minecraft:barrel` (id 26).
    Barrel = 26,
    /// `minecraft:smoker` (id 27).
    Smoker = 27,
    /// `minecraft:blast_furnace` (id 28).
    BlastFurnace = 28,
    /// `minecraft:lectern` (id 29).
    Lectern = 29,
    /// `minecraft:bell` (id 30).
    Bell = 30,
    /// `minecraft:jigsaw` (id 31).
    Jigsaw = 31,
    /// `minecraft:campfire` (id 32).
    Campfire = 32,
    /// `minecraft:beehive` (id 33).
    Beehive = 33,
    /// `minecraft:sculk_sensor` (id 34).
    SculkSensor = 34,
    /// `minecraft:calibrated_sculk_sensor` (id 35).
    CalibratedSculkSensor = 35,
    /// `minecraft:sculk_catalyst` (id 36).
    SculkCatalyst = 36,
    /// `minecraft:sculk_shrieker` (id 37).
    SculkShrieker = 37,
    /// `minecraft:chiseled_bookshelf` (id 38).
    ChiseledBookshelf = 38,
    /// `minecraft:shelf` (id 39).
    Shelf = 39,
    /// `minecraft:brushable_block` (id 40).
    BrushableBlock = 40,
    /// `minecraft:decorated_pot` (id 41).
    DecoratedPot = 41,
    /// `minecraft:crafter` (id 42).
    Crafter = 42,
    /// `minecraft:trial_spawner` (id 43).
    TrialSpawner = 43,
    /// `minecraft:vault` (id 44).
    Vault = 44,
    /// `minecraft:test_block` (id 45).
    TestBlock = 45,
    /// `minecraft:test_instance_block` (id 46).
    TestInstanceBlock = 46,
    /// `minecraft:copper_golem_statue` (id 47).
    CopperGolemStatue = 47,
    /// `minecraft:potent_sulfur` (id 48).
    PotentSulfur = 48,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`BlockEntityType::name`] is one load from a static.
/// `crate::generated::registry::BLOCK_ENTITY_TYPE` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 49] = [
    "minecraft:furnace",
    "minecraft:chest",
    "minecraft:trapped_chest",
    "minecraft:ender_chest",
    "minecraft:jukebox",
    "minecraft:dispenser",
    "minecraft:dropper",
    "minecraft:sign",
    "minecraft:hanging_sign",
    "minecraft:mob_spawner",
    "minecraft:creaking_heart",
    "minecraft:piston",
    "minecraft:brewing_stand",
    "minecraft:enchanting_table",
    "minecraft:end_portal",
    "minecraft:beacon",
    "minecraft:skull",
    "minecraft:daylight_detector",
    "minecraft:hopper",
    "minecraft:comparator",
    "minecraft:banner",
    "minecraft:structure_block",
    "minecraft:end_gateway",
    "minecraft:command_block",
    "minecraft:shulker_box",
    "minecraft:conduit",
    "minecraft:barrel",
    "minecraft:smoker",
    "minecraft:blast_furnace",
    "minecraft:lectern",
    "minecraft:bell",
    "minecraft:jigsaw",
    "minecraft:campfire",
    "minecraft:beehive",
    "minecraft:sculk_sensor",
    "minecraft:calibrated_sculk_sensor",
    "minecraft:sculk_catalyst",
    "minecraft:sculk_shrieker",
    "minecraft:chiseled_bookshelf",
    "minecraft:shelf",
    "minecraft:brushable_block",
    "minecraft:decorated_pot",
    "minecraft:crafter",
    "minecraft:trial_spawner",
    "minecraft:vault",
    "minecraft:test_block",
    "minecraft:test_instance_block",
    "minecraft:copper_golem_statue",
    "minecraft:potent_sulfur",
];

/// Discriminants ordered by name, so [`BlockEntityType::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 49 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 49] = [
    20, 26, 15, 33, 30, 28, 12, 40, 35, 32, 1, 38, 23, 19, 25, 47, 42, 10, 17, 41, 5, 6, 13, 22,
    14, 3, 0, 8, 18, 31, 4, 29, 9, 11, 48, 36, 34, 37, 39, 24, 7, 16, 27, 21, 45, 46, 2, 43, 44,
];

impl BlockEntityType {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::Furnace,
        Self::Chest,
        Self::TrappedChest,
        Self::EnderChest,
        Self::Jukebox,
        Self::Dispenser,
        Self::Dropper,
        Self::Sign,
        Self::HangingSign,
        Self::MobSpawner,
        Self::CreakingHeart,
        Self::Piston,
        Self::BrewingStand,
        Self::EnchantingTable,
        Self::EndPortal,
        Self::Beacon,
        Self::Skull,
        Self::DaylightDetector,
        Self::Hopper,
        Self::Comparator,
        Self::Banner,
        Self::StructureBlock,
        Self::EndGateway,
        Self::CommandBlock,
        Self::ShulkerBox,
        Self::Conduit,
        Self::Barrel,
        Self::Smoker,
        Self::BlastFurnace,
        Self::Lectern,
        Self::Bell,
        Self::Jigsaw,
        Self::Campfire,
        Self::Beehive,
        Self::SculkSensor,
        Self::CalibratedSculkSensor,
        Self::SculkCatalyst,
        Self::SculkShrieker,
        Self::ChiseledBookshelf,
        Self::Shelf,
        Self::BrushableBlock,
        Self::DecoratedPot,
        Self::Crafter,
        Self::TrialSpawner,
        Self::Vault,
        Self::TestBlock,
        Self::TestInstanceBlock,
        Self::CopperGolemStatue,
        Self::PotentSulfur,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 49;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:block_entity_type";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `minecraft:furnace`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `BlockEntityType` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for BlockEntityType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(BlockEntityType::ALL[48].id().0 == 48);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<BlockEntityType>() == 1);
