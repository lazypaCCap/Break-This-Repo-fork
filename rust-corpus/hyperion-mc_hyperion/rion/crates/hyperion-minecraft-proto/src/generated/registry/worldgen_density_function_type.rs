// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:worldgen/density_function_type`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:worldgen/density_function_type` (34 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`WorldgenDensityFunctionType::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`WorldgenDensityFunctionType::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum WorldgenDensityFunctionType {
    /// `minecraft:blend_alpha` (id 0).
    BlendAlpha = 0,
    /// `minecraft:blend_offset` (id 1).
    BlendOffset = 1,
    /// `minecraft:beardifier` (id 2).
    Beardifier = 2,
    /// `minecraft:old_blended_noise` (id 3).
    OldBlendedNoise = 3,
    /// `minecraft:interpolated` (id 4).
    Interpolated = 4,
    /// `minecraft:flat_cache` (id 5).
    FlatCache = 5,
    /// `minecraft:cache_2d` (id 6).
    Cache2d = 6,
    /// `minecraft:cache_once` (id 7).
    CacheOnce = 7,
    /// `minecraft:cache_all_in_cell` (id 8).
    CacheAllInCell = 8,
    /// `minecraft:blend_density` (id 9).
    BlendDensity = 9,
    /// `minecraft:noise` (id 10).
    Noise = 10,
    /// `minecraft:end_islands` (id 11).
    EndIslands = 11,
    /// `minecraft:shifted_noise` (id 12).
    ShiftedNoise = 12,
    /// `minecraft:range_choice` (id 13).
    RangeChoice = 13,
    /// `minecraft:interval_select` (id 14).
    IntervalSelect = 14,
    /// `minecraft:shift_a` (id 15).
    ShiftA = 15,
    /// `minecraft:shift_b` (id 16).
    ShiftB = 16,
    /// `minecraft:shift` (id 17).
    Shift = 17,
    /// `minecraft:clamp` (id 18).
    Clamp = 18,
    /// `minecraft:abs` (id 19).
    Abs = 19,
    /// `minecraft:square` (id 20).
    Square = 20,
    /// `minecraft:cube` (id 21).
    Cube = 21,
    /// `minecraft:half_negative` (id 22).
    HalfNegative = 22,
    /// `minecraft:quarter_negative` (id 23).
    QuarterNegative = 23,
    /// `minecraft:invert` (id 24).
    Invert = 24,
    /// `minecraft:squeeze` (id 25).
    Squeeze = 25,
    /// `minecraft:add` (id 26).
    Add = 26,
    /// `minecraft:mul` (id 27).
    Mul = 27,
    /// `minecraft:min` (id 28).
    Min = 28,
    /// `minecraft:max` (id 29).
    Max = 29,
    /// `minecraft:spline` (id 30).
    Spline = 30,
    /// `minecraft:constant` (id 31).
    Constant = 31,
    /// `minecraft:y_clamped_gradient` (id 32).
    YClampedGradient = 32,
    /// `minecraft:find_top_surface` (id 33).
    FindTopSurface = 33,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`WorldgenDensityFunctionType::name`] is one load from a static.
/// `crate::generated::registry::WORLDGEN_DENSITY_FUNCTION_TYPE` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 34] = [
    "minecraft:blend_alpha",
    "minecraft:blend_offset",
    "minecraft:beardifier",
    "minecraft:old_blended_noise",
    "minecraft:interpolated",
    "minecraft:flat_cache",
    "minecraft:cache_2d",
    "minecraft:cache_once",
    "minecraft:cache_all_in_cell",
    "minecraft:blend_density",
    "minecraft:noise",
    "minecraft:end_islands",
    "minecraft:shifted_noise",
    "minecraft:range_choice",
    "minecraft:interval_select",
    "minecraft:shift_a",
    "minecraft:shift_b",
    "minecraft:shift",
    "minecraft:clamp",
    "minecraft:abs",
    "minecraft:square",
    "minecraft:cube",
    "minecraft:half_negative",
    "minecraft:quarter_negative",
    "minecraft:invert",
    "minecraft:squeeze",
    "minecraft:add",
    "minecraft:mul",
    "minecraft:min",
    "minecraft:max",
    "minecraft:spline",
    "minecraft:constant",
    "minecraft:y_clamped_gradient",
    "minecraft:find_top_surface",
];

/// Discriminants ordered by name, so [`WorldgenDensityFunctionType::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 34 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 34] = [
    19, 26, 2, 0, 9, 1, 6, 8, 7, 18, 31, 21, 11, 33, 5, 22, 4, 14, 24, 29, 28, 27, 10, 3, 23, 13,
    17, 15, 16, 12, 30, 20, 25, 32,
];

impl WorldgenDensityFunctionType {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::BlendAlpha,
        Self::BlendOffset,
        Self::Beardifier,
        Self::OldBlendedNoise,
        Self::Interpolated,
        Self::FlatCache,
        Self::Cache2d,
        Self::CacheOnce,
        Self::CacheAllInCell,
        Self::BlendDensity,
        Self::Noise,
        Self::EndIslands,
        Self::ShiftedNoise,
        Self::RangeChoice,
        Self::IntervalSelect,
        Self::ShiftA,
        Self::ShiftB,
        Self::Shift,
        Self::Clamp,
        Self::Abs,
        Self::Square,
        Self::Cube,
        Self::HalfNegative,
        Self::QuarterNegative,
        Self::Invert,
        Self::Squeeze,
        Self::Add,
        Self::Mul,
        Self::Min,
        Self::Max,
        Self::Spline,
        Self::Constant,
        Self::YClampedGradient,
        Self::FindTopSurface,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 34;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:worldgen/density_function_type";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `minecraft:blend_alpha`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `WorldgenDensityFunctionType` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for WorldgenDensityFunctionType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(WorldgenDensityFunctionType::ALL[33].id().0 == 33);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<WorldgenDensityFunctionType>() == 1);
