// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:enchantment_effect_component_type`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:enchantment_effect_component_type` (31 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`EnchantmentEffectComponentType::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`EnchantmentEffectComponentType::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum EnchantmentEffectComponentType {
    /// `minecraft:damage_protection` (id 0).
    DamageProtection = 0,
    /// `minecraft:damage_immunity` (id 1).
    DamageImmunity = 1,
    /// `minecraft:damage` (id 2).
    Damage = 2,
    /// `minecraft:smash_damage_per_fallen_block` (id 3).
    SmashDamagePerFallenBlock = 3,
    /// `minecraft:knockback` (id 4).
    Knockback = 4,
    /// `minecraft:armor_effectiveness` (id 5).
    ArmorEffectiveness = 5,
    /// `minecraft:post_attack` (id 6).
    PostAttack = 6,
    /// `minecraft:post_piercing_attack` (id 7).
    PostPiercingAttack = 7,
    /// `minecraft:hit_block` (id 8).
    HitBlock = 8,
    /// `minecraft:item_damage` (id 9).
    ItemDamage = 9,
    /// `minecraft:equipment_drops` (id 10).
    EquipmentDrops = 10,
    /// `minecraft:location_changed` (id 11).
    LocationChanged = 11,
    /// `minecraft:tick` (id 12).
    Tick = 12,
    /// `minecraft:ammo_use` (id 13).
    AmmoUse = 13,
    /// `minecraft:projectile_piercing` (id 14).
    ProjectilePiercing = 14,
    /// `minecraft:projectile_spawned` (id 15).
    ProjectileSpawned = 15,
    /// `minecraft:projectile_spread` (id 16).
    ProjectileSpread = 16,
    /// `minecraft:projectile_count` (id 17).
    ProjectileCount = 17,
    /// `minecraft:trident_return_acceleration` (id 18).
    TridentReturnAcceleration = 18,
    /// `minecraft:fishing_time_reduction` (id 19).
    FishingTimeReduction = 19,
    /// `minecraft:fishing_luck_bonus` (id 20).
    FishingLuckBonus = 20,
    /// `minecraft:block_experience` (id 21).
    BlockExperience = 21,
    /// `minecraft:mob_experience` (id 22).
    MobExperience = 22,
    /// `minecraft:repair_with_xp` (id 23).
    RepairWithXp = 23,
    /// `minecraft:attributes` (id 24).
    Attributes = 24,
    /// `minecraft:crossbow_charge_time` (id 25).
    CrossbowChargeTime = 25,
    /// `minecraft:crossbow_charging_sounds` (id 26).
    CrossbowChargingSounds = 26,
    /// `minecraft:trident_sound` (id 27).
    TridentSound = 27,
    /// `minecraft:prevent_equipment_drop` (id 28).
    PreventEquipmentDrop = 28,
    /// `minecraft:prevent_armor_change` (id 29).
    PreventArmorChange = 29,
    /// `minecraft:trident_spin_attack_strength` (id 30).
    TridentSpinAttackStrength = 30,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`EnchantmentEffectComponentType::name`] is one load from a static.
/// `crate::generated::registry::ENCHANTMENT_EFFECT_COMPONENT_TYPE` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 31] = [
    "minecraft:damage_protection",
    "minecraft:damage_immunity",
    "minecraft:damage",
    "minecraft:smash_damage_per_fallen_block",
    "minecraft:knockback",
    "minecraft:armor_effectiveness",
    "minecraft:post_attack",
    "minecraft:post_piercing_attack",
    "minecraft:hit_block",
    "minecraft:item_damage",
    "minecraft:equipment_drops",
    "minecraft:location_changed",
    "minecraft:tick",
    "minecraft:ammo_use",
    "minecraft:projectile_piercing",
    "minecraft:projectile_spawned",
    "minecraft:projectile_spread",
    "minecraft:projectile_count",
    "minecraft:trident_return_acceleration",
    "minecraft:fishing_time_reduction",
    "minecraft:fishing_luck_bonus",
    "minecraft:block_experience",
    "minecraft:mob_experience",
    "minecraft:repair_with_xp",
    "minecraft:attributes",
    "minecraft:crossbow_charge_time",
    "minecraft:crossbow_charging_sounds",
    "minecraft:trident_sound",
    "minecraft:prevent_equipment_drop",
    "minecraft:prevent_armor_change",
    "minecraft:trident_spin_attack_strength",
];

/// Discriminants ordered by name, so [`EnchantmentEffectComponentType::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 31 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 31] = [
    13, 5, 24, 21, 25, 26, 2, 1, 0, 10, 20, 19, 8, 9, 4, 11, 22, 6, 7, 29, 28, 17, 14, 15, 16, 23,
    3, 12, 18, 27, 30,
];

impl EnchantmentEffectComponentType {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::DamageProtection,
        Self::DamageImmunity,
        Self::Damage,
        Self::SmashDamagePerFallenBlock,
        Self::Knockback,
        Self::ArmorEffectiveness,
        Self::PostAttack,
        Self::PostPiercingAttack,
        Self::HitBlock,
        Self::ItemDamage,
        Self::EquipmentDrops,
        Self::LocationChanged,
        Self::Tick,
        Self::AmmoUse,
        Self::ProjectilePiercing,
        Self::ProjectileSpawned,
        Self::ProjectileSpread,
        Self::ProjectileCount,
        Self::TridentReturnAcceleration,
        Self::FishingTimeReduction,
        Self::FishingLuckBonus,
        Self::BlockExperience,
        Self::MobExperience,
        Self::RepairWithXp,
        Self::Attributes,
        Self::CrossbowChargeTime,
        Self::CrossbowChargingSounds,
        Self::TridentSound,
        Self::PreventEquipmentDrop,
        Self::PreventArmorChange,
        Self::TridentSpinAttackStrength,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 31;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:enchantment_effect_component_type";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `minecraft:damage_protection`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `EnchantmentEffectComponentType` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for EnchantmentEffectComponentType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(EnchantmentEffectComponentType::ALL[30].id().0 == 30);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<EnchantmentEffectComponentType>() == 1);
