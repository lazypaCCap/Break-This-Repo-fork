// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:enchantment_location_based_effect_type`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:enchantment_location_based_effect_type` (16 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`EnchantmentLocationBasedEffectType::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`EnchantmentLocationBasedEffectType::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum EnchantmentLocationBasedEffectType {
    /// `minecraft:all_of` (id 0).
    AllOf = 0,
    /// `minecraft:apply_mob_effect` (id 1).
    ApplyMobEffect = 1,
    /// `minecraft:attribute` (id 2).
    Attribute = 2,
    /// `minecraft:change_item_damage` (id 3).
    ChangeItemDamage = 3,
    /// `minecraft:damage_entity` (id 4).
    DamageEntity = 4,
    /// `minecraft:explode` (id 5).
    Explode = 5,
    /// `minecraft:ignite` (id 6).
    Ignite = 6,
    /// `minecraft:apply_impulse` (id 7).
    ApplyImpulse = 7,
    /// `minecraft:apply_exhaustion` (id 8).
    ApplyExhaustion = 8,
    /// `minecraft:play_sound` (id 9).
    PlaySound = 9,
    /// `minecraft:replace_block` (id 10).
    ReplaceBlock = 10,
    /// `minecraft:replace_disk` (id 11).
    ReplaceDisk = 11,
    /// `minecraft:run_function` (id 12).
    RunFunction = 12,
    /// `minecraft:set_block_properties` (id 13).
    SetBlockProperties = 13,
    /// `minecraft:spawn_particles` (id 14).
    SpawnParticles = 14,
    /// `minecraft:summon_entity` (id 15).
    SummonEntity = 15,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`EnchantmentLocationBasedEffectType::name`] is one load from a static.
/// `crate::generated::registry::ENCHANTMENT_LOCATION_BASED_EFFECT_TYPE` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 16] = [
    "minecraft:all_of",
    "minecraft:apply_mob_effect",
    "minecraft:attribute",
    "minecraft:change_item_damage",
    "minecraft:damage_entity",
    "minecraft:explode",
    "minecraft:ignite",
    "minecraft:apply_impulse",
    "minecraft:apply_exhaustion",
    "minecraft:play_sound",
    "minecraft:replace_block",
    "minecraft:replace_disk",
    "minecraft:run_function",
    "minecraft:set_block_properties",
    "minecraft:spawn_particles",
    "minecraft:summon_entity",
];

/// Discriminants ordered by name, so [`EnchantmentLocationBasedEffectType::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 16 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 16] = [0, 8, 7, 1, 2, 3, 4, 5, 6, 9, 10, 11, 12, 13, 14, 15];

impl EnchantmentLocationBasedEffectType {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::AllOf,
        Self::ApplyMobEffect,
        Self::Attribute,
        Self::ChangeItemDamage,
        Self::DamageEntity,
        Self::Explode,
        Self::Ignite,
        Self::ApplyImpulse,
        Self::ApplyExhaustion,
        Self::PlaySound,
        Self::ReplaceBlock,
        Self::ReplaceDisk,
        Self::RunFunction,
        Self::SetBlockProperties,
        Self::SpawnParticles,
        Self::SummonEntity,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 16;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:enchantment_location_based_effect_type";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `minecraft:all_of`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `EnchantmentLocationBasedEffectType` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for EnchantmentLocationBasedEffectType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(EnchantmentLocationBasedEffectType::ALL[15].id().0 == 15);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<EnchantmentLocationBasedEffectType>() == 1);
