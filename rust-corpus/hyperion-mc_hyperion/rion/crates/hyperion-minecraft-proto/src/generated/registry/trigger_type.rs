// @generated by nix/generate-rust.py from Minecraft 26.2 (protocol 776).
// Do not edit by hand. Regenerate with: nix run .#sync-minecraft-proto

//! `minecraft:trigger_type`, as a closed Rust enum.

use crate::types::RegistryId;

/// An entry of `minecraft:trigger_type` (58 of them).
///
/// Closed on purpose. Every value this version defines is a variant, so a
/// caller cannot name one that does not exist and a `match` never needs an arm
/// for a case that cannot occur. The one place a value might not be in the set
/// is an id arriving off the wire, and [`TriggerType::from_id`] is the only door for
/// it.
///
/// The discriminant is the network id, so [`TriggerType::id`] is an `as` cast: a
/// known variant's id folds to a constant at compile time.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
#[repr(u8)]
pub enum TriggerType {
    /// `minecraft:impossible` (id 0).
    Impossible = 0,
    /// `minecraft:player_killed_entity` (id 1).
    PlayerKilledEntity = 1,
    /// `minecraft:entity_killed_player` (id 2).
    EntityKilledPlayer = 2,
    /// `minecraft:enter_block` (id 3).
    EnterBlock = 3,
    /// `minecraft:inventory_changed` (id 4).
    InventoryChanged = 4,
    /// `minecraft:recipe_unlocked` (id 5).
    RecipeUnlocked = 5,
    /// `minecraft:player_hurt_entity` (id 6).
    PlayerHurtEntity = 6,
    /// `minecraft:entity_hurt_player` (id 7).
    EntityHurtPlayer = 7,
    /// `minecraft:enchanted_item` (id 8).
    EnchantedItem = 8,
    /// `minecraft:filled_bucket` (id 9).
    FilledBucket = 9,
    /// `minecraft:brewed_potion` (id 10).
    BrewedPotion = 10,
    /// `minecraft:construct_beacon` (id 11).
    ConstructBeacon = 11,
    /// `minecraft:used_ender_eye` (id 12).
    UsedEnderEye = 12,
    /// `minecraft:summoned_entity` (id 13).
    SummonedEntity = 13,
    /// `minecraft:bred_animals` (id 14).
    BredAnimals = 14,
    /// `minecraft:location` (id 15).
    Location = 15,
    /// `minecraft:slept_in_bed` (id 16).
    SleptInBed = 16,
    /// `minecraft:cured_zombie_villager` (id 17).
    CuredZombieVillager = 17,
    /// `minecraft:villager_trade` (id 18).
    VillagerTrade = 18,
    /// `minecraft:item_durability_changed` (id 19).
    ItemDurabilityChanged = 19,
    /// `minecraft:levitation` (id 20).
    Levitation = 20,
    /// `minecraft:changed_dimension` (id 21).
    ChangedDimension = 21,
    /// `minecraft:tick` (id 22).
    Tick = 22,
    /// `minecraft:tame_animal` (id 23).
    TameAnimal = 23,
    /// `minecraft:placed_block` (id 24).
    PlacedBlock = 24,
    /// `minecraft:consume_item` (id 25).
    ConsumeItem = 25,
    /// `minecraft:effects_changed` (id 26).
    EffectsChanged = 26,
    /// `minecraft:used_totem` (id 27).
    UsedTotem = 27,
    /// `minecraft:nether_travel` (id 28).
    NetherTravel = 28,
    /// `minecraft:fishing_rod_hooked` (id 29).
    FishingRodHooked = 29,
    /// `minecraft:channeled_lightning` (id 30).
    ChanneledLightning = 30,
    /// `minecraft:shot_crossbow` (id 31).
    ShotCrossbow = 31,
    /// `minecraft:spear_mobs` (id 32).
    SpearMobs = 32,
    /// `minecraft:killed_by_arrow` (id 33).
    KilledByArrow = 33,
    /// `minecraft:hero_of_the_village` (id 34).
    HeroOfTheVillage = 34,
    /// `minecraft:voluntary_exile` (id 35).
    VoluntaryExile = 35,
    /// `minecraft:slide_down_block` (id 36).
    SlideDownBlock = 36,
    /// `minecraft:bee_nest_destroyed` (id 37).
    BeeNestDestroyed = 37,
    /// `minecraft:target_hit` (id 38).
    TargetHit = 38,
    /// `minecraft:item_used_on_block` (id 39).
    ItemUsedOnBlock = 39,
    /// `minecraft:default_block_use` (id 40).
    DefaultBlockUse = 40,
    /// `minecraft:any_block_use` (id 41).
    AnyBlockUse = 41,
    /// `minecraft:player_generates_container_loot` (id 42).
    PlayerGeneratesContainerLoot = 42,
    /// `minecraft:thrown_item_picked_up_by_entity` (id 43).
    ThrownItemPickedUpByEntity = 43,
    /// `minecraft:thrown_item_picked_up_by_player` (id 44).
    ThrownItemPickedUpByPlayer = 44,
    /// `minecraft:player_interacted_with_entity` (id 45).
    PlayerInteractedWithEntity = 45,
    /// `minecraft:player_sheared_equipment` (id 46).
    PlayerShearedEquipment = 46,
    /// `minecraft:started_riding` (id 47).
    StartedRiding = 47,
    /// `minecraft:lightning_strike` (id 48).
    LightningStrike = 48,
    /// `minecraft:using_item` (id 49).
    UsingItem = 49,
    /// `minecraft:fall_from_height` (id 50).
    FallFromHeight = 50,
    /// `minecraft:ride_entity_in_lava` (id 51).
    RideEntityInLava = 51,
    /// `minecraft:kill_mob_near_sculk_catalyst` (id 52).
    KillMobNearSculkCatalyst = 52,
    /// `minecraft:allay_drop_item_on_block` (id 53).
    AllayDropItemOnBlock = 53,
    /// `minecraft:avoid_vibration` (id 54).
    AvoidVibration = 54,
    /// `minecraft:recipe_crafted` (id 55).
    RecipeCrafted = 55,
    /// `minecraft:crafter_recipe_crafted` (id 56).
    CrafterRecipeCrafted = 56,
    /// `minecraft:fall_after_explosion` (id 57).
    FallAfterExplosion = 57,
}

/// Entry names in network-id order, so the index is the id.
///
/// A table and not a `match`, so [`TriggerType::name`] is one load from a static.
/// `crate::generated::registry::TRIGGER_TYPE` points at this same array, which is
/// what stops the two from disagreeing.
pub static NAMES_IN_ID_ORDER: [&str; 58] = [
    "minecraft:impossible",
    "minecraft:player_killed_entity",
    "minecraft:entity_killed_player",
    "minecraft:enter_block",
    "minecraft:inventory_changed",
    "minecraft:recipe_unlocked",
    "minecraft:player_hurt_entity",
    "minecraft:entity_hurt_player",
    "minecraft:enchanted_item",
    "minecraft:filled_bucket",
    "minecraft:brewed_potion",
    "minecraft:construct_beacon",
    "minecraft:used_ender_eye",
    "minecraft:summoned_entity",
    "minecraft:bred_animals",
    "minecraft:location",
    "minecraft:slept_in_bed",
    "minecraft:cured_zombie_villager",
    "minecraft:villager_trade",
    "minecraft:item_durability_changed",
    "minecraft:levitation",
    "minecraft:changed_dimension",
    "minecraft:tick",
    "minecraft:tame_animal",
    "minecraft:placed_block",
    "minecraft:consume_item",
    "minecraft:effects_changed",
    "minecraft:used_totem",
    "minecraft:nether_travel",
    "minecraft:fishing_rod_hooked",
    "minecraft:channeled_lightning",
    "minecraft:shot_crossbow",
    "minecraft:spear_mobs",
    "minecraft:killed_by_arrow",
    "minecraft:hero_of_the_village",
    "minecraft:voluntary_exile",
    "minecraft:slide_down_block",
    "minecraft:bee_nest_destroyed",
    "minecraft:target_hit",
    "minecraft:item_used_on_block",
    "minecraft:default_block_use",
    "minecraft:any_block_use",
    "minecraft:player_generates_container_loot",
    "minecraft:thrown_item_picked_up_by_entity",
    "minecraft:thrown_item_picked_up_by_player",
    "minecraft:player_interacted_with_entity",
    "minecraft:player_sheared_equipment",
    "minecraft:started_riding",
    "minecraft:lightning_strike",
    "minecraft:using_item",
    "minecraft:fall_from_height",
    "minecraft:ride_entity_in_lava",
    "minecraft:kill_mob_near_sculk_catalyst",
    "minecraft:allay_drop_item_on_block",
    "minecraft:avoid_vibration",
    "minecraft:recipe_crafted",
    "minecraft:crafter_recipe_crafted",
    "minecraft:fall_after_explosion",
];

/// Discriminants ordered by name, so [`TriggerType::from_name`] is a binary search
/// and not a scan.
///
/// Sorted here, at generation time, rather than assumed of the registry:
/// this registry's own order is not name order. A wrong order would fail as a lookup miss and read as "that
/// entry does not exist", so `tests/registry_enum.rs` puts every one of the
/// 58 names back through `from_name` rather than spot-checking.
static BY_NAME: [u8; 58] = [
    53, 41, 54, 37, 14, 10, 21, 30, 11, 25, 56, 17, 40, 26, 8, 3, 7, 2, 57, 50, 9, 29, 34, 0, 4,
    19, 39, 52, 33, 20, 48, 15, 28, 24, 42, 6, 45, 1, 46, 55, 5, 51, 31, 16, 36, 32, 47, 13, 23,
    38, 43, 44, 22, 12, 27, 49, 18, 35,
];

impl TriggerType {
    /// Every entry, in network-id order, so the index is the id.
    pub const ALL: &'static [Self] = &[
        Self::Impossible,
        Self::PlayerKilledEntity,
        Self::EntityKilledPlayer,
        Self::EnterBlock,
        Self::InventoryChanged,
        Self::RecipeUnlocked,
        Self::PlayerHurtEntity,
        Self::EntityHurtPlayer,
        Self::EnchantedItem,
        Self::FilledBucket,
        Self::BrewedPotion,
        Self::ConstructBeacon,
        Self::UsedEnderEye,
        Self::SummonedEntity,
        Self::BredAnimals,
        Self::Location,
        Self::SleptInBed,
        Self::CuredZombieVillager,
        Self::VillagerTrade,
        Self::ItemDurabilityChanged,
        Self::Levitation,
        Self::ChangedDimension,
        Self::Tick,
        Self::TameAnimal,
        Self::PlacedBlock,
        Self::ConsumeItem,
        Self::EffectsChanged,
        Self::UsedTotem,
        Self::NetherTravel,
        Self::FishingRodHooked,
        Self::ChanneledLightning,
        Self::ShotCrossbow,
        Self::SpearMobs,
        Self::KilledByArrow,
        Self::HeroOfTheVillage,
        Self::VoluntaryExile,
        Self::SlideDownBlock,
        Self::BeeNestDestroyed,
        Self::TargetHit,
        Self::ItemUsedOnBlock,
        Self::DefaultBlockUse,
        Self::AnyBlockUse,
        Self::PlayerGeneratesContainerLoot,
        Self::ThrownItemPickedUpByEntity,
        Self::ThrownItemPickedUpByPlayer,
        Self::PlayerInteractedWithEntity,
        Self::PlayerShearedEquipment,
        Self::StartedRiding,
        Self::LightningStrike,
        Self::UsingItem,
        Self::FallFromHeight,
        Self::RideEntityInLava,
        Self::KillMobNearSculkCatalyst,
        Self::AllayDropItemOnBlock,
        Self::AvoidVibration,
        Self::RecipeCrafted,
        Self::CrafterRecipeCrafted,
        Self::FallAfterExplosion,
    ];
    /// How many entries this version has, so every valid id is below it.
    pub const COUNT: usize = 58;
    /// The registry these values belong to.
    pub const REGISTRY: &'static str = "minecraft:trigger_type";

    /// The network id.
    ///
    /// `#[repr(u8)]` on the enum is what makes this an `as` cast rather
    /// than a lookup: for a variant known at compile time the whole call folds
    /// to the number.
    #[must_use]
    pub const fn id(self) -> RegistryId {
        RegistryId(self as i32)
    }

    /// The namespaced name, e.g. `minecraft:impossible`.
    ///
    /// Not `const`: it reads a static table. The direction that is const is
    /// [`Self::id`], which is the one that reaches the wire.
    #[must_use]
    pub fn name(self) -> &'static str {
        NAMES_IN_ID_ORDER[self as usize]
    }

    /// Resolve an id that arrived off the wire.
    ///
    /// The decode boundary, and the only place a `TriggerType` can fail to exist.
    /// Every other path is holding a value that is already in the set.
    #[must_use]
    pub fn from_id(id: RegistryId) -> Option<Self> {
        usize::try_from(id.0)
            .ok()
            .and_then(|index| Self::ALL.get(index).copied())
    }

    /// Resolve a name only known at run time -- one read out of a world file,
    /// or typed into a command.
    ///
    /// A name known at compile time should be the variant instead, so that a
    /// version bump dropping it fails to build at the line that wanted it
    /// rather than returning `None` on whichever tick it was next needed.
    #[must_use]
    pub fn from_name(name: &str) -> Option<Self> {
        let found = BY_NAME
            .binary_search_by(|&index| NAMES_IN_ID_ORDER[index as usize].cmp(name))
            .ok()?;
        Self::ALL.get(BY_NAME[found] as usize).copied()
    }
}

impl core::fmt::Display for TriggerType {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.name())
    }
}

// The zero-cost claim, checked by the compiler rather than by a test.
//
// A `const` context cannot call anything the compiler is unable to evaluate at
// compile time, so if `id()` were ever a table lookup or a `match` over a
// runtime value instead of a cast off the discriminant, this line would stop
// building. It is the proof, not a restatement of it.
const _: () = assert!(TriggerType::ALL[57].id().0 == 57);

// And the size, for the same reason: `#[repr(u8)]` is what makes the
// discriminant the id, and a value of this enum is that many bytes and no
// more. The old hand-written `EntityType` was a name pointer plus an `i32`,
// which is 16.
const _: () = assert!(core::mem::size_of::<TriggerType>() == 1);
